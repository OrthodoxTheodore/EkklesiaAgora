---
phase: 06-patristic-library-study-guides
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(main)/fathers/page.tsx
  - src/app/(main)/fathers/[slug]/page.tsx
  - src/app/(main)/fathers/[slug]/[textId]/page.tsx
  - src/app/(main)/fathers/search/page.tsx
  - src/components/fathers/AuthorCard.tsx
  - src/components/fathers/FathersGrid.tsx
  - src/components/fathers/PatristicReader.tsx
  - src/components/fathers/PatristicSearch.tsx
  - src/components/fathers/PatristicSkeleton.tsx
autonomous: true
requirements:
  - PAT-03
  - PAT-04
  - PAT-05
  - PAT-06

must_haves:
  truths:
    - "User can view a grid of Church Fathers organized by era on the /fathers landing page"
    - "User can filter authors by topic using the sidebar on /fathers"
    - "User can click an author card to see their bio and list of works on /fathers/[slug]"
    - "User can click a work to read its full text in the Byzantine reader on /fathers/[slug]/[textId]"
    - "User can search patristic texts by keyword on /fathers/search and see results with author and topic context"
    - "All patristic pages render in the Byzantine aesthetic (font-cinzel headings, font-garamond body, text-gold accents, bg-navy)"
  artifacts:
    - path: "src/app/(main)/fathers/page.tsx"
      provides: "Church Fathers landing page with author grid and topic filter"
      contains: "getPatristicAuthors"
    - path: "src/app/(main)/fathers/[slug]/page.tsx"
      provides: "Author detail page with bio and works list"
      contains: "getAuthor"
    - path: "src/app/(main)/fathers/[slug]/[textId]/page.tsx"
      provides: "Patristic text reader page"
      contains: "getPatristicText"
    - path: "src/app/(main)/fathers/search/page.tsx"
      provides: "Search results page for patristic texts"
      contains: "searchPatristicTexts"
    - path: "src/components/fathers/PatristicReader.tsx"
      provides: "Full-text reader component mirroring ScriptureReader"
  key_links:
    - from: "src/app/(main)/fathers/page.tsx"
      to: "src/lib/firestore/patristic.ts"
      via: "Server Component calls getPatristicAuthors"
      pattern: "getPatristicAuthors"
    - from: "src/app/(main)/fathers/[slug]/page.tsx"
      to: "src/lib/firestore/patristic.ts"
      via: "Server Component calls getAuthor + getAuthorTexts"
      pattern: "getAuthor|getAuthorTexts"
    - from: "src/app/(main)/fathers/[slug]/[textId]/page.tsx"
      to: "src/lib/firestore/patristic.ts"
      via: "Server Component calls getPatristicText"
      pattern: "getPatristicText"
    - from: "src/components/fathers/FathersGrid.tsx"
      to: "src/components/fathers/AuthorCard.tsx"
      via: "renders AuthorCard for each author"
      pattern: "AuthorCard"
---

<objective>
Build the full patristic library UI: Church Fathers landing page with author grid and topic filter, author detail pages, patristic text reader, and keyword search -- all in the Byzantine aesthetic.

Purpose: Deliver the user-facing patristic library that lets users browse, discover, and read Church Fathers texts (PAT-03, PAT-04, PAT-05, PAT-06).

Output: 4 new route pages under /fathers, 5 new components in src/components/fathers/, all wired to the Firestore query functions from Plan 06-01.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-patristic-library-study-guides/06-CONTEXT.md
@.planning/phases/06-patristic-library-study-guides/06-RESEARCH.md
@.planning/phases/06-patristic-library-study-guides/06-UI-SPEC.md
@.planning/phases/06-patristic-library-study-guides/06-01-SUMMARY.md

<interfaces>
<!-- Types and query functions created in Plan 06-01 -->

From src/lib/types/patristic.ts:
```typescript
export type PatristicEra = 'apostolic' | 'ante-nicene' | 'nicene' | 'post-nicene';
export interface PatristicText {
  textId: string; authorSlug: string; authorName: string; era: PatristicEra;
  title: string; workTitle: string; chapterOrHomily: number | null;
  topics: OrthodoxCategory[]; source: string; sortOrder: number;
  body: string; searchKeywords: string[];
}
export interface PatristicAuthor {
  authorSlug: string; name: string; era: PatristicEra; eraLabel: string;
  feastDay: string | null; keyContribution: string; bio: string; sortOrder: number;
}
```

From src/lib/firestore/patristic.ts:
```typescript
export async function getPatristicAuthors(): Promise<PatristicAuthor[]>
export async function getAuthor(slug: string): Promise<PatristicAuthor | null>
export async function getAuthorTexts(authorSlug: string): Promise<PatristicText[]>
export async function getPatristicText(textId: string): Promise<PatristicText | null>
export async function searchPatristicTexts(query: string, limit?: number): Promise<PatristicText[]>
```

From src/lib/constants/categories.ts:
```typescript
export const ORTHODOX_CATEGORIES = ['Divine Liturgy', 'Holy Scripture', 'Holy Fathers', ...] as const;
export type OrthodoxCategory = (typeof ORTHODOX_CATEGORIES)[number];
```

From src/components/scripture/ScriptureReader.tsx (pattern to mirror):
- Props: initialVerses, bookMeta, currentChapter, allBooks
- Uses font-cinzel for labels, font-garamond for body, text-gold for accents
- Has Prev/Next navigation with border-t border-gold/[0.15]
- Error state: "This chapter could not be loaded. Please try refreshing the page."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Church Fathers landing page with author grid, topic filter sidebar, and author detail page</name>
  <files>
    src/app/(main)/fathers/page.tsx,
    src/app/(main)/fathers/[slug]/page.tsx,
    src/components/fathers/AuthorCard.tsx,
    src/components/fathers/FathersGrid.tsx,
    src/components/fathers/PatristicSkeleton.tsx
  </files>
  <read_first>
    src/lib/types/patristic.ts,
    src/lib/firestore/patristic.ts,
    src/lib/constants/categories.ts,
    src/app/(main)/scripture/page.tsx,
    src/app/(main)/scripture/[book]/[chapter]/page.tsx,
    src/components/scripture/ScriptureReader.tsx,
    src/components/calendar/SaintCard.tsx,
    .planning/phases/06-patristic-library-study-guides/06-UI-SPEC.md
  </read_first>
  <action>
**1. Create `src/components/fathers/AuthorCard.tsx`** (presentational, no 'use client' needed if used inside a client component):

Props: `{ author: PatristicAuthor }`. Renders as a `<Link>` to `/fathers/${author.authorSlug}`. Layout per UI-SPEC FatherCard contract:
- Outer: `bg-navy-mid border border-gold/[0.15] rounded-[6px] p-4 hover:border-gold/40 transition-colors focus-visible:ring-2 focus-visible:ring-gold/60 block`
- Era label: `<span className="font-cinzel text-xs uppercase tracking-widest text-text-mid">{author.eraLabel}</span>`
- Author name: `<h3 className="font-cinzel text-sm text-gold mt-1">{author.name}</h3>`
- Key contribution: `<p className="font-garamond text-sm text-text-light mt-2 line-clamp-2">{author.keyContribution}</p>`
- Feast day (if non-null): `<p className="font-garamond text-xs text-text-mid mt-2">Feast: {author.feastDay}</p>`

**2. Create `src/components/fathers/FathersGrid.tsx`** ('use client'):

Props: `{ authors: PatristicAuthor[] }`. State: `selectedTopic: OrthodoxCategory | null` (null = show all).

Import `ORTHODOX_CATEGORIES` from `@/lib/constants/categories`.

**Topic filter sidebar** (left column on lg): Vertical stack of pills. Each pill:
- Inactive: `font-cinzel text-xs uppercase tracking-widest text-text-mid border border-gold/[0.15] rounded px-3 py-1.5 hover:text-gold hover:border-gold/40 transition-colors`
- Active: `font-cinzel text-xs uppercase tracking-widest text-navy bg-gold rounded px-3 py-1.5`
- An "All" pill at the top that clears filter (selectedTopic = null).

**Filtering logic:** When a topic is selected, filter authors to only show those whose `authorSlug` appears in at least one seeded text with that topic. BUT since we only have authors (not texts) passed as props, and we don't want to fetch all texts client-side, use a simpler approach: show all authors regardless of topic filter, and the filter narrows based on era grouping display. Actually, the simplest correct approach: pass a `topicToAuthors` map from the Server Component. The server can query all texts, extract `topics` and `authorSlug` pairs, build a `Map<OrthodoxCategory, Set<authorSlug>>`, and pass it as a serialized prop `topicIndex: Record<string, string[]>`. Then FathersGrid filters by checking `topicIndex[selectedTopic]?.includes(author.authorSlug)`.

**Author grid** (right column): Group authors by era. For each era group:
- Era divider: `<h2 className="font-cinzel text-xs uppercase tracking-widest text-text-mid mt-8 mb-3">{eraDisplayName}</h2>`
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
- Render `<AuthorCard>` for each filtered author in that era.

Era display names: `{ apostolic: 'Apostolic Fathers', 'ante-nicene': 'Ante-Nicene Fathers', nicene: 'Nicene Fathers', 'post-nicene': 'Post-Nicene Fathers' }`.

**Empty state** (when filter yields nothing): Heading `"No Fathers in this category"`, body `"Try selecting a different topic or browse all Church Fathers."` per UI-SPEC.

**Layout:** `lg:grid lg:grid-cols-[240px_1fr] gap-8`. On mobile, sidebar above grid.

**3. Create `src/components/fathers/PatristicSkeleton.tsx`** (simple shimmer loading):

Renders 6 placeholder cards with `animate-pulse bg-navy-mid rounded-[6px] h-32`. Same pattern as ScriptureSkeleton.tsx.

**4. Create `src/app/(main)/fathers/page.tsx`** (Server Component):

Import `getPatristicAuthors` and `getAuthorTexts` (or a bulk function) from `@/lib/firestore/patristic`. Alternatively, to build the topic index, fetch all authors and all texts in parallel:

```typescript
import { getPatristicAuthors } from '@/lib/firestore/patristic';
import { getAdminFirestore } from '@/lib/firebase/admin';
import type { PatristicText } from '@/lib/types/patristic';

export default async function FathersPage() {
  const [authors, textsSnap] = await Promise.all([
    getPatristicAuthors(),
    getAdminFirestore().collection('patristic_texts').select('authorSlug', 'topics').get(),
  ]);

  // Build topic index: { topicName: [authorSlug1, authorSlug2, ...] }
  const topicIndex: Record<string, string[]> = {};
  textsSnap.docs.forEach(doc => {
    const data = doc.data() as Pick<PatristicText, 'authorSlug' | 'topics'>;
    (data.topics || []).forEach(topic => {
      if (!topicIndex[topic]) topicIndex[topic] = [];
      if (!topicIndex[topic].includes(data.authorSlug)) {
        topicIndex[topic].push(data.authorSlug);
      }
    });
  });

  return (
    <div className="max-w-7xl mx-auto px-4 py-8">
      <h1 className="font-cinzel text-gold text-xl uppercase tracking-widest mb-6">
        Church Fathers
      </h1>
      {/* PatristicSearch link/bar - simple link to /fathers/search */}
      <Link href="/fathers/search"
        className="block w-full bg-navy-mid border border-gold/[0.15] rounded text-text-mid font-garamond text-base px-4 py-2 mb-8 hover:border-gold/40 transition-colors"
        aria-label="Search Church Fathers texts">
        Search the Fathers...
      </Link>
      <FathersGrid authors={authors} topicIndex={topicIndex} />
    </div>
  );
}
```

**5. Create `src/app/(main)/fathers/[slug]/page.tsx`** (Server Component):

```typescript
import { getAuthor, getAuthorTexts } from '@/lib/firestore/patristic';
import { notFound } from 'next/navigation';
import Link from 'next/link';

export default async function AuthorPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const [author, texts] = await Promise.all([getAuthor(slug), getAuthorTexts(slug)]);
  if (!author) notFound();

  return (
    <div className="max-w-2xl mx-auto px-4 py-8">
      <span className="font-cinzel text-xs uppercase tracking-widest text-text-mid mb-2 block">
        {author.eraLabel}
      </span>
      <h1 className="font-cinzel text-gold text-xl uppercase tracking-widest mb-4">
        {author.name}
      </h1>
      <p className="font-garamond text-base text-text-light leading-relaxed mb-2">{author.bio}</p>
      {author.feastDay && (
        <p className="font-garamond text-sm text-text-mid mb-8">Feast Day: {author.feastDay}</p>
      )}
      <h2 className="font-cinzel text-xs uppercase tracking-widest text-text-mid mt-8 mb-4">Works</h2>
      <div className="space-y-0">
        {texts.map(text => (
          <Link
            key={text.textId}
            href={`/fathers/${slug}/${text.textId}`}
            className="block border-b border-gold/[0.15] py-4 hover:bg-navy-light/30 transition-colors px-2 -mx-2 rounded"
          >
            <span className="font-cinzel text-sm text-gold hover:text-gold-bright">{text.title}</span>
            {text.chapterOrHomily && (
              <span className="font-garamond text-xs text-text-mid ml-2">(Chapter {text.chapterOrHomily})</span>
            )}
            <div className="flex gap-2 mt-1">
              {text.topics.map(t => (
                <span key={t} className="font-cinzel text-xs text-text-mid">{t}</span>
              ))}
            </div>
          </Link>
        ))}
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>npx jest --testPathPatterns="patristic|PatristicReader" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/fathers/page.tsx contains `getPatristicAuthors`
    - src/app/(main)/fathers/page.tsx contains `font-cinzel text-gold text-xl uppercase tracking-widest`
    - src/app/(main)/fathers/page.tsx contains `Church Fathers`
    - src/app/(main)/fathers/[slug]/page.tsx contains `getAuthor`
    - src/app/(main)/fathers/[slug]/page.tsx contains `getAuthorTexts`
    - src/app/(main)/fathers/[slug]/page.tsx contains `notFound`
    - src/components/fathers/AuthorCard.tsx contains `bg-navy-mid border border-gold/[0.15]`
    - src/components/fathers/AuthorCard.tsx contains `font-cinzel text-sm text-gold`
    - src/components/fathers/FathersGrid.tsx contains `'use client'`
    - src/components/fathers/FathersGrid.tsx contains `ORTHODOX_CATEGORIES`
    - src/components/fathers/FathersGrid.tsx contains `No Fathers in this category`
    - src/components/fathers/PatristicSkeleton.tsx contains `animate-pulse`
    - All existing tests still pass
  </acceptance_criteria>
  <done>Church Fathers landing page renders author grid grouped by era with functional topic filter sidebar. Author detail page shows bio, feast day, and ordered works list with links to text reader. All pages use Byzantine aesthetic tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Create patristic text reader page and search page with component tests</name>
  <files>
    src/app/(main)/fathers/[slug]/[textId]/page.tsx,
    src/app/(main)/fathers/search/page.tsx,
    src/components/fathers/PatristicReader.tsx,
    src/components/fathers/PatristicSearch.tsx
  </files>
  <read_first>
    src/lib/types/patristic.ts,
    src/lib/firestore/patristic.ts,
    src/components/scripture/ScriptureReader.tsx,
    src/components/scripture/ScriptureSearch.tsx,
    src/app/(main)/scripture/[book]/[chapter]/page.tsx,
    tests/components/PatristicReader.test.tsx,
    tests/components/ScriptureReader.test.tsx,
    .planning/phases/06-patristic-library-study-guides/06-UI-SPEC.md
  </read_first>
  <action>
**1. Create `src/components/fathers/PatristicReader.tsx`** ('use client'):

Props: `{ text: PatristicText; authorSlug: string; prevText: PatristicText | null; nextText: PatristicText | null }`.

Layout per UI-SPEC `/fathers/[slug]/[textId]` contract:
- Breadcrumb: `<nav className="font-cinzel text-xs text-text-mid mb-4"><Link href="/fathers" className="hover:text-gold">Church Fathers</Link> <span> / </span> <Link href={"/fathers/" + authorSlug} className="hover:text-gold">{text.authorName}</Link></nav>`
- Title: `<h1 className="font-cinzel text-gold text-xl uppercase tracking-widest mb-2">{text.title}</h1>`
- Chapter indicator (if non-null): `<p className="font-cinzel text-xs text-text-mid mb-1">{text.workTitle} - Chapter {text.chapterOrHomily}</p>`
- Attribution: `<p className="font-garamond text-xs text-text-mid italic mb-8">From the {text.era === 'ante-nicene' ? 'Ante-Nicene' : text.era === 'nicene' ? 'Nicene and Post-Nicene' : text.era === 'post-nicene' ? 'Nicene and Post-Nicene' : 'Ante-Nicene'} Fathers series, Philip Schaff edition. Public domain.</p>`
- Body: `<article className="font-garamond text-base text-text-light leading-relaxed whitespace-pre-line">{text.body}</article>`
- Prev/Next navigation: Same pattern as ScriptureReader -- `<div className="flex justify-between mt-8 pt-4 border-t border-gold/[0.15]">`. Show prev link if prevText exists, next link if nextText exists. Links go to `/fathers/${authorSlug}/${prevText.textId}` and `/fathers/${authorSlug}/${nextText.textId}`. Text: `"<- Previous"` / `"Next ->"` in `font-cinzel text-xs text-gold`.

**2. Create `src/app/(main)/fathers/[slug]/[textId]/page.tsx`** (Server Component):

```typescript
import { getPatristicText, getAuthorTexts } from '@/lib/firestore/patristic';
import { PatristicReader } from '@/components/fathers/PatristicReader';
import { notFound } from 'next/navigation';

export default async function TextReaderPage({
  params,
}: {
  params: Promise<{ slug: string; textId: string }>;
}) {
  const { slug, textId } = await params;
  const [text, authorTexts] = await Promise.all([
    getPatristicText(textId),
    getAuthorTexts(slug),
  ]);
  if (!text) notFound();

  const idx = authorTexts.findIndex(t => t.textId === textId);
  const prevText = idx > 0 ? authorTexts[idx - 1] : null;
  const nextText = idx < authorTexts.length - 1 ? authorTexts[idx + 1] : null;

  return (
    <div className="max-w-2xl mx-auto px-4 py-8">
      <PatristicReader text={text} authorSlug={slug} prevText={prevText} nextText={nextText} />
    </div>
  );
}
```

**3. Create `src/components/fathers/PatristicSearch.tsx`** ('use client'):

Props: `{ initialResults: PatristicText[]; initialQuery: string }`.

State: `query` (string, initialized from initialQuery), `results` (PatristicText[], initialized from initialResults).

Form with search input + submit button. On submit, navigate to `/fathers/search?q=${encodeURIComponent(query)}` using router.push (let the Server Component refetch). This avoids client-side Firestore reads.

Search input: `bg-navy-mid border border-gold/[0.15] rounded text-text-light font-garamond text-base px-4 py-2 focus:outline-none focus:border-gold/60 w-full` with `aria-label="Search Church Fathers texts"`.

Results list: each result renders as a link to `/fathers/${result.authorSlug}/${result.textId}`:
- Title: `font-cinzel text-sm text-gold`
- Author + era: `font-garamond text-xs text-text-mid`
- Topics: joined with " / " in `font-cinzel text-xs text-text-mid`
- Body excerpt: first 150 characters of body + "..." in `font-garamond text-sm text-text-light mt-1`

Empty state (no results): Heading "No texts match your search", body "Try different keywords, or browse by author." per UI-SPEC.

**4. Create `src/app/(main)/fathers/search/page.tsx`** (Server Component):

```typescript
import { searchPatristicTexts } from '@/lib/firestore/patristic';
import { PatristicSearch } from '@/components/fathers/PatristicSearch';

export default async function SearchPage({
  searchParams,
}: {
  searchParams: Promise<{ q?: string }>;
}) {
  const { q } = await searchParams;
  const query = q?.trim() || '';
  const results = query ? await searchPatristicTexts(query) : [];

  return (
    <div className="max-w-2xl mx-auto px-4 py-8">
      <h1 className="font-cinzel text-gold text-xl uppercase tracking-widest mb-6">
        Search the Fathers
      </h1>
      <PatristicSearch initialResults={results} initialQuery={query} />
    </div>
  );
}
```

**5. Flesh out test stubs** in `tests/components/PatristicReader.test.tsx` to actually assert against the real component:
- Update mock data to match the actual PatristicText interface
- Ensure `font-cinzel` and `text-gold` assertions match the real h1 title rendering
- Ensure `font-garamond` assertion matches the article body
- Ensure breadcrumb renders "Church Fathers" link
- Ensure prev/next navigation renders when prevText/nextText provided
  </action>
  <verify>
    <automated>npx jest --testPathPatterns="PatristicReader|patristic" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/fathers/[slug]/[textId]/page.tsx contains `getPatristicText`
    - src/app/(main)/fathers/[slug]/[textId]/page.tsx contains `PatristicReader`
    - src/app/(main)/fathers/[slug]/[textId]/page.tsx contains `notFound`
    - src/app/(main)/fathers/search/page.tsx contains `searchPatristicTexts`
    - src/app/(main)/fathers/search/page.tsx contains `Search the Fathers`
    - src/components/fathers/PatristicReader.tsx contains `'use client'`
    - src/components/fathers/PatristicReader.tsx contains `font-cinzel text-gold text-xl`
    - src/components/fathers/PatristicReader.tsx contains `font-garamond text-base text-text-light leading-relaxed`
    - src/components/fathers/PatristicReader.tsx contains `border-t border-gold/[0.15]`
    - src/components/fathers/PatristicReader.tsx contains `Church Fathers`
    - src/components/fathers/PatristicReader.tsx contains `Public domain`
    - src/components/fathers/PatristicSearch.tsx contains `'use client'`
    - src/components/fathers/PatristicSearch.tsx contains `No texts match your search`
    - src/components/fathers/PatristicSearch.tsx contains `aria-label`
    - tests/components/PatristicReader.test.tsx passes (exits 0)
  </acceptance_criteria>
  <done>Patristic text reader renders full text with breadcrumb, attribution, prev/next navigation in Byzantine aesthetic. Search page accepts keyword queries via URL params, shows results with author/topic context, and handles empty state. Component tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPatterns="patristic|PatristicReader|StudyGuide" --no-coverage` exits 0
- All 4 route pages exist under src/app/(main)/fathers/
- All 5 components exist under src/components/fathers/
- `npx next build 2>&1 | grep -i error` shows no build errors in fathers routes
</verification>

<success_criteria>
1. /fathers landing page shows author grid grouped by era with topic filter sidebar
2. /fathers/[slug] shows author bio, feast day, and works list
3. /fathers/[slug]/[textId] shows full patristic text with breadcrumb, attribution, prev/next nav
4. /fathers/search accepts keyword and returns matching texts with context
5. All pages use font-cinzel headings, font-garamond body, text-gold accents, bg-navy backgrounds
6. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-patristic-library-study-guides/06-02-SUMMARY.md`
</output>
