---
phase: 03-video-hub-moderation
plan: 06
type: execute
wave: 1
depends_on: ["03-03", "03-04"]
files_modified: ["src/app/(main)/channel/[handle]/page.tsx"]
autonomous: true
gap_closure: true
requirements: [CHAN-03]

must_haves:
  truths:
    - "Channel page at /channel/[handle] renders VideoCard components for each published video"
  artifacts:
    - path: "src/app/(main)/channel/[handle]/page.tsx"
      provides: "Channel detail page with VideoCard grid"
      contains: "VideoCard"
  key_links:
    - from: "src/app/(main)/channel/[handle]/page.tsx"
      to: "src/components/video/VideoCard.tsx"
      via: "import VideoCard"
      pattern: "import VideoCard from '@/components/video/VideoCard'"
---

<objective>
Replace the placeholder div block in the channel detail page with the actual VideoCard component.

Purpose: Close the single verified gap from 03-VERIFICATION.md — the channel page renders plain divs with title/category text instead of the VideoCard component that was created in plan 03-04. This partially blocks CHAN-03.
Output: Updated channel page that renders VideoCard for each published video.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-video-hub-moderation/03-VERIFICATION.md

<interfaces>
<!-- VideoCard component interface from src/components/video/VideoCard.tsx -->

```typescript
// 'use client' component — safe to import in Server Components (Next.js handles the boundary)
import type { Video } from '@/lib/types/video';

interface VideoCardProps {
  video: Video;
  showInlinePlayback?: boolean;
}

export default function VideoCard({ video, showInlinePlayback = false }: VideoCardProps): JSX.Element;
```

<!-- Existing usage pattern from src/app/(main)/videos/page.tsx (Server Component) -->
```typescript
import VideoCard from '@/components/video/VideoCard';
// ...
{videos.map((video) => (
  <VideoCard key={video.videoId} video={video} />
))}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire VideoCard into channel detail page</name>
  <files>src/app/(main)/channel/[handle]/page.tsx</files>
  <read_first>
    - src/app/(main)/channel/[handle]/page.tsx (the file being modified — current placeholder at lines 168-175)
    - src/components/video/VideoCard.tsx (the component to import — confirms props interface)
    - src/app/(main)/videos/page.tsx (reference: existing Server Component that imports VideoCard)
  </read_first>
  <action>
In `src/app/(main)/channel/[handle]/page.tsx`:

1. Add import on line 9 (after the existing `import type { Channel, Video } from '@/lib/types/video';` line):
   ```
   import VideoCard from '@/components/video/VideoCard';
   ```

2. Replace lines 167-176 (the map callback with placeholder div) with:
   ```tsx
   {videos.map((video) => (
     <VideoCard key={video.videoId} video={video} />
   ))}
   ```

   This removes the entire block:
   ```tsx
   {videos.map((video) => (
     // Placeholder for VideoCard — will be replaced in plan 03-04
     <div
       key={video.videoId}
       className="bg-navy-mid border border-gold/[0.15] rounded-[6px] p-4"
     >
       <p className="font-cinzel text-sm text-text-light truncate">{video.title}</p>
       <p className="font-garamond text-xs text-text-mid mt-1">{video.category}</p>
     </div>
   ))}
   ```

   And replaces it with:
   ```tsx
   {videos.map((video) => (
     <VideoCard key={video.videoId} video={video} />
   ))}
   ```

No other changes to the file.
  </action>
  <verify>
    <automated>grep -n "import VideoCard from '@/components/video/VideoCard'" "src/app/(main)/channel/[handle]/page.tsx" && grep -n "<VideoCard" "src/app/(main)/channel/[handle]/page.tsx" && ! grep -n "Placeholder for VideoCard" "src/app/(main)/channel/[handle]/page.tsx" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(main)/channel/[handle]/page.tsx` contains the line `import VideoCard from '@/components/video/VideoCard';`
    - `src/app/(main)/channel/[handle]/page.tsx` contains `<VideoCard key={video.videoId} video={video} />`
    - `src/app/(main)/channel/[handle]/page.tsx` does NOT contain `Placeholder for VideoCard`
    - `src/app/(main)/channel/[handle]/page.tsx` does NOT contain `video.category}</p>` inside a plain div (the old placeholder markup)
    - `npx next build` compiles without errors (or `npx tsc --noEmit` passes)
  </acceptance_criteria>
  <done>Channel detail page renders VideoCard for each published video in the grid. The placeholder comment and div block are fully removed. CHAN-03 is fully satisfied.</done>
</task>

</tasks>

<verification>
1. `grep -c "import VideoCard" src/app/(main)/channel/[handle]/page.tsx` returns `1`
2. `grep -c "<VideoCard" src/app/(main)/channel/[handle]/page.tsx` returns `1`
3. `grep -c "Placeholder for VideoCard" src/app/(main)/channel/[handle]/page.tsx` returns `0`
4. TypeScript compilation succeeds: `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
The channel detail page at `/channel/[handle]` renders the same VideoCard component used on `/videos` — with thumbnail, duration chip, avatar, title link, view count, relative date, and category chip — instead of a plain text placeholder div. CHAN-03 is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-video-hub-moderation/03-06-SUMMARY.md`
</output>
