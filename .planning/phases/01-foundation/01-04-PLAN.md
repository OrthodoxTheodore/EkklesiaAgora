---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ui/theme.test.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - DES-01
  - DES-02
  - DES-03

must_haves:
  truths:
    - "All 3 DES-01 theme test assertions pass with real checks against globals.css content"
    - "No test.todo() placeholders remain in tests/ui/theme.test.ts"
    - "Theme test file validates color tokens, font tokens, and spacing token from globals.css"
  artifacts:
    - path: "tests/ui/theme.test.ts"
      provides: "Real assertions for Byzantine theme DES-01 coverage"
      contains: "expect"
      min_lines: 20
  key_links:
    - from: "tests/ui/theme.test.ts"
      to: "src/styles/globals.css"
      via: "fs.readFileSync to read CSS content"
      pattern: "readFileSync.*globals\\.css"
---

<objective>
Fill in the 3 test.todo() placeholders in tests/ui/theme.test.ts with real assertions that verify the Byzantine theme tokens defined in globals.css.

Purpose: Close the single code gap identified in 01-VERIFICATION.md — DES-01 has no automated test coverage because the theme test stubs were never filled in during Plans 01-01 through 01-03.

Output: A complete theme test file with passing assertions for all color tokens, font tokens, and spacing tokens.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md

<interfaces>
<!-- The test reads globals.css as a text file and asserts token presence -->

From src/styles/globals.css (the file under test):
```css
@theme {
  --color-navy:        #0d1b2e;
  --color-navy-mid:    #152338;
  --color-navy-light:  #1e3352;
  --color-gold:        #c9a84c;
  --color-gold-bright: #e8c96a;
  --color-gold-dim:    #8a6f30;
  --color-gold-pale:   #f0dfa0;
  --color-text-light:  #e8dfc8;
  --color-text-mid:    #b8a888;
  --color-crimson:     #8b1a1a;

  --font-cinzel:     'Cinzel', serif;
  --font-cinzel-dec: 'Cinzel Decorative', serif;
  --font-garamond:   'EB Garamond', serif;

  --spacing-nav: 70px;
}
```

From tests/ui/theme.test.ts (current state — the gap):
```typescript
import '@testing-library/jest-dom';

describe('Byzantine theme (DES-01)', () => {
  test.todo('globals.css contains all required color tokens');
  test.todo('globals.css contains font-family tokens for cinzel, cinzel-dec, garamond');
  test.todo('globals.css contains --spacing-nav: 70px');
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace test.todo() stubs with real assertions in theme.test.ts</name>
  <files>tests/ui/theme.test.ts</files>
  <read_first>
    - tests/ui/theme.test.ts
    - src/styles/globals.css
    - jest.config.ts
  </read_first>
  <action>
Replace the entire contents of tests/ui/theme.test.ts with real assertions. The approach:

1. Use Node.js `fs.readFileSync` and `path.resolve` to read `src/styles/globals.css` as a string at the top of the describe block (or in a beforeAll).

2. Replace `test.todo('globals.css contains all required color tokens')` with a real test that asserts the CSS string contains ALL 10 color tokens with their exact hex values:
   - `--color-navy` contains `#0d1b2e`
   - `--color-navy-mid` contains `#152338`
   - `--color-navy-light` contains `#1e3352`
   - `--color-gold` contains `#c9a84c`
   - `--color-gold-bright` contains `#e8c96a`
   - `--color-gold-dim` contains `#8a6f30`
   - `--color-gold-pale` contains `#f0dfa0`
   - `--color-text-light` contains `#e8dfc8`
   - `--color-text-mid` contains `#b8a888`
   - `--color-crimson` contains `#8b1a1a`

   Use `expect(css).toContain('--color-navy')` and `expect(css).toContain('#0d1b2e')` style assertions (or combine into `expect(css).toMatch(/--color-navy:\s*#0d1b2e/)` regex patterns for tighter coupling).

3. Replace `test.todo('globals.css contains font-family tokens...')` with a real test asserting:
   - `--font-cinzel` token present with `'Cinzel', serif`
   - `--font-cinzel-dec` token present with `'Cinzel Decorative', serif`
   - `--font-garamond` token present with `'EB Garamond', serif`

4. Replace `test.todo('globals.css contains --spacing-nav: 70px')` with:
   - `expect(css).toMatch(/--spacing-nav:\s*70px/)`

5. Keep the `import '@testing-library/jest-dom'` at the top (it does not hurt even though these tests use Node assertions, and removing it could break if jest.setup.ts expects it).

Do NOT use jsdom rendering or component mounting — these are pure file content assertions using fs.readFileSync. This is appropriate because the theme tokens are defined in a CSS file, not computed at runtime.
  </action>
  <verify>
    <automated>cd "C:/Users/Owner/Downloads/Claude Projects/Ekklesia_Agora" && npx jest tests/ui/theme.test.ts --verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/ui/theme.test.ts contains zero `test.todo(` calls
    - tests/ui/theme.test.ts contains at least 3 `test(` or `it(` blocks with `expect` assertions
    - `npx jest tests/ui/theme.test.ts` exits with code 0 and reports 3 tests passing
    - Test 1 validates all 10 color tokens (navy, navy-mid, navy-light, gold, gold-bright, gold-dim, gold-pale, text-light, text-mid, crimson) with hex values
    - Test 2 validates all 3 font tokens (cinzel, cinzel-dec, garamond)
    - Test 3 validates --spacing-nav: 70px
  </acceptance_criteria>
  <done>All 3 DES-01 theme tests pass with real assertions. No test.todo() placeholders remain. The gap identified in 01-VERIFICATION.md is closed.</done>
</task>

</tasks>

<verification>
1. Run `npx jest tests/ui/theme.test.ts --verbose` — all 3 tests pass
2. Run `grep -c "test.todo" tests/ui/theme.test.ts` — returns 0
3. Run `npx jest --testPathPattern="(auth|roles|claims|theme|nav|middleware)" --passWithNoTests` — full Phase 1 test suite still green
</verification>

<success_criteria>
- Zero test.todo() placeholders remain in tests/ui/theme.test.ts
- All 3 theme tests pass with real assertions reading globals.css
- Full Phase 1 test suite remains green (no regressions)
- DES-01 requirement now has automated test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
