---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - src/styles/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/firebase/client.ts
  - src/lib/firebase/admin.ts
  - src/middleware.ts
  - .env.local
  - .env.example
  - .gitignore
  - jest.config.ts
  - jest.setup.ts
  - tests/auth/register.test.ts
  - tests/auth/session.test.ts
  - tests/auth/reset.test.ts
  - tests/auth/logout.test.ts
  - tests/middleware/public-routes.test.ts
  - tests/auth/claims.test.ts
  - tests/auth/roles.test.ts
  - tests/ui/theme.test.ts
  - tests/components/nav.test.tsx
autonomous: true
requirements:
  - DES-01
  - DES-02
  - DES-03
  - AUTH-05

must_haves:
  truths:
    - "Next.js 15 dev server starts without errors"
    - "Byzantine theme colors render correctly (navy background, gold accents)"
    - "Cinzel and EB Garamond fonts load without layout shift"
    - "Navigation bar is sticky with correct height and responsive hamburger on mobile"
    - "Public pages are accessible without authentication"
    - "Firebase client SDK initializes without errors"
    - "Jest test infrastructure runs and all stubs pass with --passWithNoTests"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Byzantine @theme tokens matching mockup CSS variables"
      contains: "--color-navy"
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, AuthProvider shell, nav"
      min_lines: 30
    - path: "src/lib/firebase/client.ts"
      provides: "Firebase client SDK singleton"
      exports: ["default"]
    - path: "src/lib/firebase/admin.ts"
      provides: "Firebase Admin SDK singleton"
      exports: ["getAdminAuth", "getAdminFirestore"]
    - path: "src/middleware.ts"
      provides: "next-firebase-auth-edge middleware with public/private route split"
      min_lines: 30
    - path: "src/components/nav/Navbar.tsx"
      provides: "Sticky top navigation with responsive hamburger"
      min_lines: 20
    - path: "jest.config.ts"
      provides: "Jest configuration with jsdom, path aliases, and TypeScript support"
      min_lines: 10
    - path: "jest.setup.ts"
      provides: "Jest setup with jest-dom matchers"
      min_lines: 1
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/styles/globals.css"
      via: "CSS import"
      pattern: "import.*globals"
    - from: "src/app/layout.tsx"
      to: "src/components/nav/Navbar.tsx"
      via: "component render"
      pattern: "<Navbar"
    - from: "src/middleware.ts"
      to: "next-firebase-auth-edge"
      via: "authMiddleware import"
      pattern: "import.*next-firebase-auth-edge"

user_setup:
  - service: firebase
    why: "Authentication and database backend"
    env_vars:
      - name: NEXT_PUBLIC_FIREBASE_API_KEY
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: NEXT_PUBLIC_FIREBASE_PROJECT_ID
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: NEXT_PUBLIC_FIREBASE_APP_ID
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: FIREBASE_PROJECT_ID
        source: "Same as NEXT_PUBLIC_FIREBASE_PROJECT_ID"
      - name: FIREBASE_CLIENT_EMAIL
        source: "Firebase Console -> Project Settings -> Service accounts -> Generate new private key -> client_email field"
      - name: FIREBASE_PRIVATE_KEY
        source: "Firebase Console -> Project Settings -> Service accounts -> Generate new private key -> private_key field (wrap in double quotes in .env.local)"
      - name: COOKIE_SECRET_CURRENT
        source: "Generate a random 32+ character string (e.g., openssl rand -hex 32)"
      - name: COOKIE_SECRET_PREVIOUS
        source: "Generate a random 32+ character string (e.g., openssl rand -hex 32)"
---

<objective>
Initialize the Next.js 15 project with Firebase integration, Tailwind v4 Byzantine theme system, font configuration, app shell with responsive navigation, the next-firebase-auth-edge middleware skeleton, and the Wave 0 test infrastructure for the entire phase.

Purpose: Establish the complete project foundation so that all subsequent plans can build auth flows and role management on top of a working, styled application shell — with test scaffolding in place for behavioral verification from the start.

Output: A running Next.js 15 dev server with Byzantine-themed landing page, sticky responsive navigation, Firebase SDK initialized (client + admin), middleware configured for public/private route splitting, and Jest test infrastructure with stub files covering all Phase 1 requirements.
</objective>

<execution_context>
@C:/Users/morga/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/morga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

Source files to reference for design language:
@index (23).html — Full Byzantine CSS design system with color variables, component patterns, typography
@Ekklesia_Agora.jpg — Brand logo for navigation and hero
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test infrastructure — Jest config and all test stubs</name>
  <files>jest.config.ts, jest.setup.ts, tests/auth/register.test.ts, tests/auth/session.test.ts, tests/auth/reset.test.ts, tests/auth/logout.test.ts, tests/middleware/public-routes.test.ts, tests/auth/claims.test.ts, tests/auth/roles.test.ts, tests/ui/theme.test.ts, tests/components/nav.test.tsx</files>
  <action>
1. **jest.config.ts** — Base Jest configuration:
   - Use `ts-jest` or `next/jest` (prefer `next/jest` since we are in a Next.js project — it handles SWC transforms automatically)
   - Set `testEnvironment: 'jsdom'`
   - Configure `moduleNameMapper` for the `@/*` path alias: `{ '^@/(.*)$': '<rootDir>/src/$1' }`
   - Set `setupFilesAfterSetup: ['<rootDir>/jest.setup.ts']`
   - Include `testMatch: ['<rootDir>/tests/**/*.test.{ts,tsx}']`

2. **jest.setup.ts** — Jest setup file:
   - Import `@testing-library/jest-dom` to add custom DOM matchers (toBeInTheDocument, toHaveClass, etc.)

3. **Install ts-jest if needed** — the dev dependencies (jest, @types/jest, @testing-library/react, @testing-library/jest-dom, jest-environment-jsdom) are already in Task 1's npm install. If using `next/jest`, no additional install is needed. If using `ts-jest`, run `npm install --save-dev ts-jest`.

4. **Create all 11 test stub files.** Each stub should:
   - Import relevant testing utilities (`@testing-library/react`, `@testing-library/jest-dom`)
   - Contain a `describe` block named after the requirement it covers
   - Contain one or more `test.todo()` or `it.todo()` placeholders describing the behavioral assertions that will be implemented when the corresponding production code is built
   - NOT contain any actual assertions yet — these are scaffolds for Plans 01-02 and 01-03 to fill in

   Specific stubs:

   a. **tests/auth/register.test.ts** (AUTH-01):
      - `describe('Registration flow (AUTH-01)')` with todos:
        - `test.todo('renders registration form with email, password, confirm password fields')`
        - `test.todo('shows validation error for weak password')`
        - `test.todo('shows validation error for mismatched passwords')`
        - `test.todo('calls createUserWithEmailAndPassword on valid submit')`

   b. **tests/auth/session.test.ts** (AUTH-02):
      - `describe('Session persistence (AUTH-02)')` with todos:
        - `test.todo('POST /api/login sets HttpOnly session cookie')`
        - `test.todo('authenticated user stays logged in after page refresh')`

   c. **tests/auth/reset.test.ts** (AUTH-03):
      - `describe('Password reset (AUTH-03)')` with todos:
        - `test.todo('renders reset password form with email field')`
        - `test.todo('calls sendPasswordResetEmail on valid submit')`
        - `test.todo('shows success message without revealing email existence')`

   d. **tests/auth/logout.test.ts** (AUTH-04):
      - `describe('Logout (AUTH-04)')` with todos:
        - `test.todo('logout clears client auth state')`
        - `test.todo('POST /api/logout clears session cookie')`
        - `test.todo('user is redirected to home after logout')`

   e. **tests/middleware/public-routes.test.ts** (AUTH-05):
      - `describe('Public route access (AUTH-05)')` with todos:
        - `test.todo('unauthenticated user can access / without redirect')`
        - `test.todo('unauthenticated user accessing /dashboard is redirected to /login')`
        - `test.todo('authenticated user accessing /login is redirected away')`
        - `test.todo('static assets and _next paths are excluded from middleware')`

   f. **tests/auth/claims.test.ts** (AUTH-06, AUTH-07):
      - `describe('Role claims (AUTH-06, AUTH-07)')` with todos:
        - `test.todo('setUserRole sets custom claims via Admin SDK')`
        - `test.todo('setUserRole logs to roleAuditLog collection')`
        - `test.todo('super admin can set any role level')`
        - `test.todo('admin can set up to moderator but not admin or super admin')`
        - `test.todo('non-admin caller is rejected')`

   g. **tests/auth/roles.test.ts** (AUTH-08):
      - `describe('Role hierarchy (AUTH-08)')` with todos:
        - `test.todo('ROLE_LEVELS has correct integer hierarchy')`
        - `test.todo('isAtLeast returns true when user level >= required')`
        - `test.todo('isAtLeast returns false when user level < required')`
        - `test.todo('guest (0) < registered (1) < moderator (2) < admin (3) < superAdmin (4)')`

   h. **tests/ui/theme.test.ts** (DES-01):
      - `describe('Byzantine theme (DES-01)')` with todos:
        - `test.todo('globals.css contains all required color tokens')`
        - `test.todo('globals.css contains font-family tokens for cinzel, cinzel-dec, garamond')`
        - `test.todo('globals.css contains --spacing-nav: 70px')`

   i. **tests/components/nav.test.tsx** (DES-02):
      - `describe('Navigation (DES-02)')` with todos:
        - `test.todo('Navbar renders with fixed positioning and correct height')`
        - `test.todo('Navbar shows Sign In and Register for unauthenticated users')`
        - `test.todo('Navbar shows hamburger menu on mobile viewport')`

5. **Add npm script** to package.json: `"test": "jest"` (if not already present from create-next-app).
  </action>
  <verify>
    <automated>cd "C:/Users/morga/OneDrive/Desktop/Ekklesia Agora" && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Jest config and setup files created. All 11 test stub files exist with descriptive todo placeholders. Running `npx jest --passWithNoTests` exits 0 with no failures. Test infrastructure is ready for Plans 01-02 and 01-03 to fill in assertions.</done>
</task>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project with Tailwind v4 and all dependencies</name>
  <files>package.json, tsconfig.json, postcss.config.mjs, .env.local, .env.example, .gitignore, src/styles/globals.css</files>
  <action>
1. Initialize the Next.js 15 project in the current directory (which contains only the HTML mockup and logo image). Run:
   `npx create-next-app@latest . --typescript --eslint --app --src-dir --import-alias "@/*"`
   When prompted, select NO for Tailwind (we add v4 manually) and YES for App Router. If create-next-app refuses to run in a non-empty directory, move the two existing files (index (23).html, Ekklesia_Agora.jpg) to a temp location, run create-next-app, then move them back into the public/ directory.

2. Install all dependencies:
   ```
   npm install firebase firebase-admin next-firebase-auth-edge react-hook-form zod @hookform/resolvers
   npm install tailwindcss @tailwindcss/postcss postcss
   npm install --save-dev jest @types/jest @testing-library/react @testing-library/jest-dom jest-environment-jsdom
   ```

3. Delete any `postcss.config.js` created by create-next-app. Create `postcss.config.mjs`:
   ```js
   const config = { plugins: { "@tailwindcss/postcss": {} } };
   export default config;
   ```

4. Delete any `tailwind.config.js` or `tailwind.config.ts` if created — Tailwind v4 uses CSS-first config only.

5. Replace `src/styles/globals.css` (or `src/app/globals.css` — wherever create-next-app puts it; move to `src/styles/globals.css`) with the full Byzantine @theme configuration:
   - `@import "tailwindcss";`
   - `@theme` block with all colors: navy (#0d1b2e), navy-mid (#152338), navy-light (#1e3352), gold (#c9a84c), gold-bright (#e8c96a), gold-dim (#8a6f30), gold-pale (#f0dfa0), text-light (#e8dfc8), text-mid (#b8a888), crimson (#8b1a1a)
   - Font family tokens: cinzel, cinzel-dec, garamond
   - Nav spacing token: --spacing-nav: 70px
   - Gold grid overlay `body::before` pseudo-element matching the mockup pattern
   - Base body styles via `@layer base` — `bg-navy text-text-light font-garamond`

6. Create `.env.example` listing all required env vars (from user_setup) with placeholder values and comments.
   Create `.env.local` with the same structure but empty values — user will fill these in.
   Ensure `.gitignore` includes `.env.local`.

7. Move `Ekklesia_Agora.jpg` into `public/` directory. Move `index (23).html` into `docs/` directory (reference only, not served).
  </action>
  <verify>
    <automated>cd "C:/Users/morga/OneDrive/Desktop/Ekklesia Agora" && npx next build 2>&1 | head -20 && npx jest tests/ui/theme.test.ts --passWithNoTests 2>&1 | tail -3</automated>
  </verify>
  <done>Next.js 15 project initialized with Tailwind v4 Byzantine theme, all dependencies installed, postcss.config.mjs configured, env var templates created, mockup assets organized. Theme test stub runs without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create app shell — root layout, fonts, navigation, landing page, Firebase SDKs, middleware</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/components/nav/Navbar.tsx, src/components/nav/MobileMenu.tsx, src/components/auth/AuthProvider.tsx, src/lib/firebase/client.ts, src/lib/firebase/admin.ts, src/lib/firebase/roles.ts, src/middleware.ts</files>
  <action>
1. **src/lib/firebase/client.ts** — Firebase client SDK singleton:
   - Import `initializeApp`, `getApps`, `getApp` from `firebase/app`
   - Read config from `process.env.NEXT_PUBLIC_FIREBASE_*` env vars
   - Export default app instance with `getApps().length === 0` guard

2. **src/lib/firebase/admin.ts** — Firebase Admin SDK singleton (server-only):
   - Add `import 'server-only'` at top to prevent client import
   - Import from `firebase-admin/app`, `firebase-admin/auth`, `firebase-admin/firestore`
   - Initialize with cert from env vars, `.replace(/\\n/g, '\n')` on private key
   - `getApps().length === 0` guard for hot-reload safety
   - Export `getAdminAuth()` and `getAdminFirestore()` functions

3. **src/lib/firebase/roles.ts** — Role constants (framework-agnostic for DES-03):
   - Export `ROLE_LEVELS` object: `{ guest: 0, registered: 1, moderator: 2, admin: 3, superAdmin: 4 }`
   - Export `RoleLevel` type
   - Export `ROLE_NAMES` reverse map for display
   - Export helper functions: `isAtLeast(userLevel: number, requiredLevel: number): boolean`
   - NO Next.js or Firebase imports — pure logic only (supports future React Native sharing per DES-03)

4. **src/components/auth/AuthProvider.tsx** — Client component ("use client"):
   - Create React context `AuthContext` holding `{ user: User | null, loading: boolean }`
   - On mount, call `onIdTokenChanged` from `firebase/auth` to track auth state
   - Export `useAuth()` hook for consuming components
   - During loading, children render normally (no full-page spinner — content is public-first per AUTH-05)

5. **src/components/nav/Navbar.tsx** — Client component ("use client"):
   - Sticky top nav: `fixed top-0 w-full h-[70px] z-50` with navy gradient background, gold-dim bottom border
   - Left: Logo image (`/Ekklesia_Agora.jpg`) + site name in Cinzel Decorative
   - Center: Nav links (Home, placeholder links for future phases) in Cinzel uppercase with letter-spacing
   - Right: Auth state conditional — if guest: "Sign In" (btn-outline style) + "Register" (btn-gold style); if authenticated: avatar dropdown with Profile/Settings/Logout
   - Avatar dropdown is a simple client-side toggle (useState), not a headless UI library
   - Mobile (below md breakpoint): hide center nav links and right auth section, show hamburger icon button
   - Hamburger opens MobileMenu component

6. **src/components/nav/MobileMenu.tsx** — Client component:
   - Slide-in or dropdown menu panel on navy-mid background
   - Contains same nav links + auth buttons as desktop
   - Close button or tap-outside-to-close

7. **src/app/layout.tsx** — Root layout:
   - Import Cinzel, Cinzel_Decorative, EB_Garamond from `next/font/google` with CSS variable bindings
   - Apply font variables to `<html>` className
   - `<body className="bg-navy font-garamond text-text-light pt-[70px]">` (pt for sticky nav offset)
   - Wrap children in `<AuthProvider>`
   - Render `<Navbar />` above children
   - Import globals.css

8. **src/app/page.tsx** — Landing page (Server Component):
   - Hero section: Large heading "Ekklesia Agora" in Cinzel Decorative, subheading "A Gathering Place for the Eastern Orthodox Community" in EB Garamond
   - Gold-bordered card section with 2-3 feature preview cards (placeholder content: "Orthodox Video", "The Agora", "Scripture Library") using the mockup's card pattern (navy-mid bg, gold border at 0.15 opacity, 6px radius)
   - Registration CTA button ("Join the Community") styled as btn-gold
   - All content accessible without auth (AUTH-05)

9. **src/middleware.ts** — next-firebase-auth-edge middleware:
   - Follow the exact pattern from RESEARCH.md Pattern 1
   - `PRIVATE_PATHS = ['/dashboard', '/admin']`
   - `AUTH_PATHS = ['/login', '/register', '/reset-password']`
   - `handleValidToken`: redirect away from auth pages if logged in
   - `handleInvalidToken`: redirect to login only for private paths; public paths pass through (AUTH-05)
   - `handleError`: log and pass through
   - Matcher excludes `_next` and static files
   - **Defense-in-depth note:** This middleware handles routing only. Data security enforced in Firestore rules and Server Components (CVE-2025-29927 mitigation).
  </action>
  <verify>
    <automated>cd "C:/Users/morga/OneDrive/Desktop/Ekklesia Agora" && npx next build 2>&1 | tail -5 && npx jest tests/middleware/public-routes.test.ts tests/components/nav.test.tsx --passWithNoTests 2>&1 | tail -3</automated>
  </verify>
  <done>Dev server starts, landing page renders with Byzantine styling (navy background, gold accents, Cinzel headings, EB Garamond body), sticky navigation shows with responsive hamburger on mobile viewport, Firebase SDKs initialize, middleware routes public paths without authentication. Middleware and nav test stubs run green.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Visit http://localhost:3000 — landing page renders with navy background, gold text, correct fonts
3. Browser DevTools confirm: `--color-navy: #0d1b2e` and `--color-gold: #c9a84c` in computed styles
4. Resize to mobile width — hamburger menu appears, desktop nav hides
5. No console errors related to Firebase initialization (env vars may be empty but SDK should not crash on page load)
6. Visit http://localhost:3000/dashboard — redirects to /login (middleware working)
7. `npx jest --passWithNoTests` exits 0 — all test stubs recognized, no failures
</verification>

<success_criteria>
- Next.js 15 project builds and serves successfully
- Byzantine design system fully implemented in Tailwind v4 @theme
- Fonts (Cinzel, Cinzel Decorative, EB Garamond) load via next/font
- Sticky responsive navigation with mobile hamburger menu
- Firebase client and admin SDKs initialized with singleton pattern
- Middleware configured for public/private route splitting
- Landing page accessible without authentication (AUTH-05)
- All code in /lib is framework-agnostic where possible (DES-03)
- Jest config and 11 test stub files created and runnable (Wave 0 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
