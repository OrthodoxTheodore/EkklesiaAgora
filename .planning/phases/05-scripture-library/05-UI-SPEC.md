---
phase: 5
slug: scripture-library
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 5 — UI Design Contract: Scripture Library

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom Tailwind v4 Byzantine design system (established Phase 1) |
| Preset | not applicable |
| Component library | none — custom primitives in `src/components/ui/` (Button, Card, Input) |
| Icon library | Inline SVG (project pattern — no external icon library installed) |
| Font | Cinzel (headings, labels, CTAs), EB Garamond (body, verse text, prose) |

Source: `src/styles/globals.css`, `src/components/ui/`, REQUIREMENTS.md DES-01

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Verse number superscript gap, inline icon gaps |
| sm | 8px | Compact element spacing, between verse number and verse text |
| md | 16px | Default element spacing, padding on search input, card padding |
| lg | 24px | Section padding, gap between book list rows |
| xl | 32px | Layout gaps, spacing between search bar and results |
| 2xl | 48px | Major section breaks (OT / NT section divider) |
| 3xl | 64px | Page-level top/bottom padding |

Exceptions:
- Reading column max-width: 65ch (prose width constraint — not a spacing token, but a layout rule). Source: CONTEXT.md locked decision.
- Prev/Next navigation buttons at chapter bottom use `py-4` (16px) — standard scale value, adequate touch target.

Source: Standard 8-point scale (default); CONTEXT.md "narrow prose width (~65ch)" decision

---

## Typography

| Role | Font | Size | Weight | Line Height |
|------|------|------|--------|-------------|
| Page heading (book name, chapter heading) | Cinzel | 20px (text-xl) | 400 (normal — Cinzel renders bold at 400) | 1.2 |
| Verse text (body) | EB Garamond | 18px (text-lg) | 400 (regular) | 1.75 |
| Label / UI text (chapter count, testament label, attribution, search group headers) | Cinzel | 13px (text-xs, tracking-widest uppercase) | 400 | 1.4 |
| Verse number superscript | EB Garamond | 11px (text-[11px]) | 400 | n/a (inline superscript) |

Notes:
- Verse numbers are rendered as superscript at 11px, color `text-gold`, inline with verse text. Source: CONTEXT.md "superscript inline" decision.
- Search result group headers (book name + match count) use the 13px label style — Cinzel uppercase tracking-widest — consistent with all other Cinzel label usage.
- Exactly 2 font weights are declared: 400 (regular) across all roles. Cinzel's optical weight reads as display-weight at 400; no semibold needed for this reading-focused phase.
- EB Garamond body at 18px/1.75 line-height is prescribed for scripture reading comfort — one size larger than the 16px standard to improve long-form readability.

Source: REQUIREMENTS.md LIB-03 ("EB Garamond body, Cinzel headings"), `src/styles/globals.css` token definitions, CONTEXT.md reader layout decisions

---

## Color

| Role | Value | Token | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `#0d1b2e` | `bg-navy` | Page background, reader column background |
| Secondary (30%) | `#152338` | `bg-navy-mid` | Book list cards, search results container, navigation panel |
| Accent (10%) | `#c9a84c` | `text-gold` / `border-gold` | Reserved list below |
| Destructive | `#8b1a1a` | `text-crimson` | Not used in Phase 5 (read-only phase — no destructive actions) |

Accent (`#c9a84c`) reserved for:
1. Verse numbers (superscript inline in chapter reader)
2. Book names in the book list (`/scripture` landing page)
3. Chapter headings in the reader (e.g., "Genesis 1")
4. Active/highlighted search keyword match (background tint: `bg-gold/[0.15]`, text remains `text-text-light`)
5. Prev/Next chapter navigation link text
6. Testament section labels ("Old Testament — Brenton Septuagint", "New Testament — Eastern Orthodox Bible")
7. EOB attribution line in the reader
8. ReadingRef links from the calendar (already established pattern: `text-gold hover:underline font-garamond`)
9. "Navigate" button (outline variant: `border-gold text-gold`) — same as existing Button outline variant

All other text: `text-text-light` (`#e8dfc8`) for primary prose; `text-text-mid` (`#b8a888`) for secondary labels (chapter count below book name, placeholder text in search).

Source: `src/styles/globals.css` tokens, `src/components/ui/Button.tsx` variants, CLAUDE.md Byzantine aesthetic rules, CONTEXT.md decisions

---

## Component Inventory

New components to build for Phase 5 (mirror existing patterns):

| Component | Type | Pattern Source |
|-----------|------|---------------|
| `ScriptureReader` | Client Component | `CalendarDayView.tsx` |
| `VerseList` | Presentational | `SaintCard.tsx` (card sub-content pattern) |
| `BookNavigator` | Client Component | Native `<select>` elements (JurisdictionDropdown pattern from Phase 2) |
| `ScriptureSearch` | Client Component | Search input from Agora feed (`src/components/agora/`) |
| `ScriptureSkeleton` | Presentational | `CalendarSkeleton.tsx` — `bg-navy-light animate-pulse rounded` blocks |
| `ScriptureLanding` | Server Component page | `calendar/page.tsx` layout pattern |
| `ChapterPage` | Server Component page | `calendar/page.tsx` — `max-w-2xl mx-auto px-4 py-8` wrapper |

Existing components to reuse without modification:
- `src/components/ui/Button.tsx` — "Navigate" button (outline variant), Prev/Next navigation
- `src/components/ui/Card.tsx` — Book list entries (`bg-navy-mid border border-gold/[0.15] rounded-[6px]`)
- `src/components/ui/Input.tsx` — Scripture search bar
- `src/components/calendar/ReadingRef.tsx` — Activate from stub to live `<Link>` (Phase 5 task)
- `src/components/nav/Navbar.tsx` — Add "Scripture" link (modification, not replacement)

---

## Interaction Contract

### Scripture Landing Page (`/scripture`)

1. **Search bar** at top — keyword or reference input. Placeholder: "Search Scripture (e.g., grace, John 3:16)". On submit: if input matches `REF_PATTERN` (`Book chapter:verse`), navigate directly to chapter page with anchor; otherwise render grouped results below the search bar.
2. **Book list** below search bar — two sections separated by a section label. OT books first (49 entries), NT books second (27 entries). Each entry: book name (Cinzel, gold, 16px) + chapter count (Cinzel, text-mid, 13px, e.g., "50 chapters"). Card layout using `Card` component.
3. **No pagination** on book list — all 76 books rendered in one pass (static list).

### Chapter Reader (`/scripture/[book]/[chapter]`)

1. **Page heading**: book name + chapter number in Cinzel gold (`font-cinzel text-gold text-xl uppercase tracking-widest`). Testament label on same line or directly below in `text-text-mid text-xs tracking-widest uppercase` (e.g., "Old Testament — Brenton Septuagint").
2. **Verse rendering**: Continuous prose. Verse number as `<sup>` in `text-gold text-[11px]` followed by verse text in `font-garamond text-lg text-text-light leading-[1.75]`. No line breaks between verses — paragraph-style reading.
3. **Verse highlight on anchor navigation**: When URL contains `#verse-N`, the target verse receives a `bg-gold/[0.15]` background highlight that fades out after 2 seconds using a CSS transition (`transition-colors duration-[2000ms]`). No bounce or scroll-to animation — browser default anchor scroll is sufficient.
4. **Navigate button**: Fixed or inline at top of reader. Uses Button outline variant. On click: opens a panel (not a modal) below the button with two native `<select>` elements — one for book (all 76 books in OT/NT optgroup sections), one for chapter (1 to N based on selected book). Submitting navigates to the selected chapter. No always-visible sidebar — collapsed by default.
5. **Prev/Next chapter navigation**: Two links at the bottom of the verse list — "← Previous Chapter" and "Next Chapter →". Cinzel, gold, 13px (`text-xs`), `py-4` vertical padding. Previous hidden on chapter 1; Next hidden on final chapter.
6. **EOB attribution**: Small text below NT chapter content only. Exact copy: "New Testament text from the Eastern Orthodox Bible (EOB), Patriarchal Text of 1904. Used with permission for non-commercial Orthodox purposes." Style: `font-garamond text-xs text-text-mid`.

### Search Results

1. Results grouped by book. Group header: book name in Cinzel gold 13px (`text-xs`) uppercase tracking-widest, followed by match count in parentheses in `text-text-mid` (e.g., "John (7 results)").
2. Each result: verse reference (e.g., "John 3:16") in `text-gold text-sm font-cinzel` followed by verse text in `font-garamond text-sm text-text-light`. Matching keyword wrapped in `<mark>` with `bg-gold/[0.15] text-text-light` (no bold, highlight only).
3. Clicking a result: navigate to `/scripture/[book]/[chapter]#verse-[n]`.
4. Maximum 30 results shown per search (Firestore query limit). If 30 results returned, show: "Showing first 30 results. Refine your search for more specific matches." in `text-text-mid text-sm font-garamond`.

### Loading State

`ScriptureSkeleton` displayed via `<Suspense fallback={<ScriptureSkeleton />}>` on the chapter reader page. Three `bg-navy-light animate-pulse rounded` blocks: one tall block (simulates heading, 40px height), one very tall block (simulates verse column, 320px height), one short block (simulates navigation links, 24px height). Source: `CalendarSkeleton.tsx` pattern.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (book list entry) | (no CTA button — book entries are direct links) |
| Navigate button label | "Navigate" |
| Prev chapter link | "← Previous Chapter" |
| Next chapter link | "Next Chapter →" |
| Search input placeholder | "Search Scripture (e.g., grace, John 3:16)" |
| Search button label | "Search" |
| Empty state heading (no search results) | "No verses matched that search" |
| Empty state body | "No verses matched your search. Try a different word or a reference like "John 3:16"." |
| Error state (chapter failed to load) | "This chapter could not be loaded. Please try refreshing the page." |
| Search result limit notice | "Showing first 30 results. Refine your search for more specific matches." |
| Reference direct-navigate notice | (none needed — direct navigation is silent, no confirmation) |
| EOB attribution (NT chapters only) | "New Testament text from the Eastern Orthodox Bible (EOB), Patriarchal Text of 1904. Used with permission for non-commercial Orthodox purposes." |
| Page title (landing) | "Scripture Library" |
| OT section label | "Old Testament — Brenton Septuagint" |
| NT section label | "New Testament — Eastern Orthodox Bible" |
| Navbar link label | "Scripture" |
| Destructive confirmation | Not applicable — Phase 5 is entirely read-only. No write actions, no destructive actions. |

Source: CONTEXT.md decisions (testament labels, attribution requirement), REQUIREMENTS.md LIB-03 to LIB-05, project tone (Orthodox, reverent)

---

## Accessibility Contract

- Verse list container: `role="article"` with `aria-label="{bookName} Chapter {n}"`.
- Each verse: `id="verse-{n}"` for anchor navigation; no explicit ARIA role needed (inline text).
- Search input: `<label>` with `htmlFor` wired to input id. Label text: "Search Scripture".
- Navigate panel: `role="dialog"` equivalent behavior using `aria-expanded` on the trigger button; panel receives focus on open.
- Skeleton: `aria-busy="true"` and `aria-label="Loading scripture"` — mirrors `CalendarSkeleton.tsx`.
- Highlighted verse on anchor nav: `aria-current="true"` on the highlighted verse `<span>` until the fade completes.
- Book list: rendered as `<ul>` with `<li>` per book entry for screen reader traversal.
- Prev/Next links: `aria-label="Previous chapter: {bookName} {n-1}"` and `aria-label="Next chapter: {bookName} {n+1}"` for context.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| Third-party | none declared | not applicable |

No third-party component registries are used in Phase 5. All components are custom-built against the established Byzantine design system. The only new devDependency is `usfm-js` v3.4.3 — a parsing utility used exclusively in seed scripts, not in the UI bundle.

---

## Design Decisions Pre-Populated From Upstream

| Decision | Source |
|----------|--------|
| EB Garamond body text, Cinzel headings | REQUIREMENTS.md LIB-03, CLAUDE.md |
| Navy `#0d1b2e` + gold `#c9a84c` color system | `src/styles/globals.css`, CLAUDE.md |
| Verse numbers as superscript inline | CONTEXT.md locked decision |
| Narrow prose column (~65ch) centered | CONTEXT.md locked decision |
| Prev/Next chapter nav at bottom | CONTEXT.md locked decision |
| Collapsed "Navigate" button for book/chapter selector | CONTEXT.md locked decision |
| Testament label near book title | CONTEXT.md locked decision |
| OT then NT canonical order on landing page | CONTEXT.md locked decision |
| Book name (Cinzel gold) + chapter count on landing | CONTEXT.md locked decision |
| Search bar on landing page | CONTEXT.md locked decision |
| Search results grouped by book | CONTEXT.md locked decision |
| Verse text only in results (no inline context) | CONTEXT.md locked decision |
| Reference detection → direct navigation | CONTEXT.md locked decision |
| EOB attribution in UI | CONTEXT.md locked decision |
| Verse highlight animation/style | CONTEXT.md — Claude's discretion (resolved: bg-gold/[0.15] fade) |
| Loading skeleton design | CONTEXT.md — Claude's discretion (resolved: CalendarSkeleton pattern) |
| Empty state for no search results | CONTEXT.md — Claude's discretion (resolved above) |
| Navigation dropdown animation | CONTEXT.md — Claude's discretion (resolved: no animation, native select elements) |
| Server Component + Client Component split | RESEARCH.md architecture patterns, `calendar/page.tsx` template |
| `(main)` route group for new pages | RESEARCH.md project structure |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
