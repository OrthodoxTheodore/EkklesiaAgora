---
phase: 02-social-core
plan: "04"
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/app/(main)/agora/page.tsx
  - src/components/agora/FeedClient.tsx
  - src/components/agora/PostCard.tsx
  - src/components/agora/ComposeBox.tsx
  - src/components/agora/CategoryFilterTabs.tsx
  - src/components/agora/BlockingSkeletons.tsx
  - src/components/agora/LinkPreviewCard.tsx
  - src/app/(main)/dashboard/page.tsx
autonomous: true
requirements: [AGRA-01, AGRA-02, AGRA-03, AGRA-05, AGRA-10, CAT-01, CAT-02]

must_haves:
  truths:
    - "A user can see their activity feed at /agora with posts from followed users"
    - "A user can compose and submit a new text post with optional photo"
    - "A user can like/unlike a post with optimistic UI update"
    - "Post cards show jurisdiction badge, category chip, avatar, handle, timestamp, like/comment counts"
    - "Feed can be filtered by category using horizontal scrollable tabs"
    - "AI category suggestion appears during post composition"
    - "Feed uses infinite scroll with cursor pagination"
    - "A user can search posts by keyword using a search input above the feed"
  artifacts:
    - path: "src/app/(main)/agora/page.tsx"
      provides: "Agora feed Server Component"
      contains: "getTokens"
    - path: "src/components/agora/FeedClient.tsx"
      provides: "Client-side feed with pagination and category filter"
      contains: "loadMorePosts"
    - path: "src/components/agora/PostCard.tsx"
      provides: "Post card component per UI-SPEC"
      contains: "toggleLike"
    - path: "src/components/agora/ComposeBox.tsx"
      provides: "Compose form with AI category classification"
      contains: "firebase/ai"
    - path: "src/components/agora/CategoryFilterTabs.tsx"
      provides: "Horizontal scrollable filter tabs"
      contains: "ORTHODOX_CATEGORIES"
  key_links:
    - from: "src/components/agora/FeedClient.tsx"
      to: "Firestore userFeed subcollection"
      via: "getDocs paginated query"
      pattern: "collection\\(db, 'users'.*'userFeed'\\)"
    - from: "src/components/agora/ComposeBox.tsx"
      to: "src/app/actions/posts.ts"
      via: "createPost Server Action call"
      pattern: "createPost\\("
    - from: "src/components/agora/PostCard.tsx"
      to: "src/app/actions/likes.ts"
      via: "toggleLike Server Action call"
      pattern: "toggleLike\\("
---

<objective>
Build the Agora feed page with all feed UI components: PostCard, ComposeBox with AI category classification, CategoryFilterTabs, infinite scroll, and skeleton loaders.

Purpose: The Agora is the social heart of Ekklesia. This plan delivers the complete feed reading and posting experience.
Output: /agora page with compose box, category filters, infinite scroll feed, post cards with like/comment interactions.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-social-core/02-CONTEXT.md
@.planning/phases/02-social-core/02-RESEARCH.md
@.planning/phases/02-social-core/02-UI-SPEC.md
@.planning/phases/02-social-core/02-01-SUMMARY.md
@.planning/phases/02-social-core/02-02-SUMMARY.md
@.planning/phases/02-social-core/02-03-SUMMARY.md

<interfaces>
From src/app/actions/posts.ts:
```typescript
export async function createPost(uid: string, data: { text: string; imageUrl: string | null; category: string; commentsRestricted: 'all' | 'followers'; linkPreview: LinkPreview | null }): Promise<{ success: boolean; postId?: string; error?: string }>;
export async function deletePost(uid: string, postId: string): Promise<{ success: boolean; error?: string }>;
export async function editPost(uid: string, postId: string, data: { text: string; category: string; commentsRestricted: 'all' | 'followers' }): Promise<{ success: boolean; error?: string }>;
```

From src/app/actions/likes.ts:
```typescript
export async function toggleLike(uid: string, postId: string): Promise<{ liked: boolean }>;
```

From src/app/actions/linkPreview.ts:
```typescript
export async function fetchLinkPreview(url: string): Promise<LinkPreview | null>;
```

From src/lib/types/social.ts:
```typescript
export interface Post { postId, authorUid, authorHandle, authorDisplayName, authorAvatarUrl, authorJurisdictionId, authorRoleLevel, text, imageUrl, category, searchKeywords, likeCount, commentCount, commentsRestricted, linkPreview, createdAt, updatedAt, isEdited }
export interface LinkPreview { url, title, description, imageUrl, siteName }
```

From src/lib/constants/categories.ts:
```typescript
export const ORTHODOX_CATEGORIES = ['Divine Liturgy', ..., 'Spiritual Life'] as const;
```

From src/lib/constants/jurisdictions.ts:
```typescript
export function getJurisdictionLabel(id: string): string;
```

From src/components/ui/Button.tsx:
```typescript
export const Button: React.FC<{ variant?: 'gold' | 'outline'; ... }>;
```

From src/components/ui/Card.tsx:
```typescript
export function Card({ children, className }: CardProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agora page, FeedClient with infinite scroll, and ComposeBox with AI classification</name>
  <files>
    src/app/(main)/agora/page.tsx,
    src/components/agora/FeedClient.tsx,
    src/components/agora/ComposeBox.tsx,
    src/components/agora/CategoryFilterTabs.tsx,
    src/components/agora/BlockingSkeletons.tsx,
    src/app/(main)/dashboard/page.tsx
  </files>
  <read_first>
    src/app/(main)/dashboard/page.tsx,
    src/lib/firebase/client.ts,
    src/components/ui/Card.tsx,
    src/components/ui/Button.tsx,
    src/components/ui/Input.tsx,
    src/lib/constants/categories.ts,
    src/lib/types/social.ts,
    .planning/phases/02-social-core/02-UI-SPEC.md
  </read_first>
  <action>
**1. Create `src/app/(main)/agora/page.tsx`** — Server Component:

- Auth check: `getTokens(await cookies(), authConfig)`, redirect to `/login` if no tokens
- Extract `uid` from `tokens.decodedToken.uid`
- Render `FeedClient uid={uid}` as the main content

**2. Update `src/app/(main)/dashboard/page.tsx`**:

- Change the page to redirect to `/agora` using `redirect('/agora')` from `next/navigation`
- Keep the auth check; after auth check, just `redirect('/agora')`
- Remove the old "features coming soon" card content

**3. Create `src/components/agora/CategoryFilterTabs.tsx`** — Client Component:

Per UI-SPEC:
- Horizontal scrollable row, `overflow-x-auto` with hidden scrollbar (`scrollbar-hide` or `-webkit-scrollbar: none`)
- Pills: "All" first, then all 10 ORTHODOX_CATEGORIES from `@/lib/constants/categories`
- Active pill: `text-gold border-b-2 border-gold`
- Inactive pill: `text-text-mid hover:text-text-light`
- Each pill: `font-cinzel text-xs uppercase tracking-widest px-3 py-2 whitespace-nowrap cursor-pointer`
- Row container: `px-4 py-2 border-b border-gold/[0.10]`
- Use `role="tablist"` on container, `role="tab"` and `aria-selected` on each pill
- Props: `activeCategory: string | null`, `onCategoryChange: (category: string | null) => void`
- "All" maps to `null` category

**4. Create `src/components/agora/BlockingSkeletons.tsx`** — Client Component:

- 3 skeleton PostCard shapes: `animate-pulse bg-navy-light` blocks
- Match PostCard layout proportions: avatar circle (36px), handle bar, text bars, button row
- Container: `aria-busy="true"` and `aria-label="Loading posts"`

**5. Create `src/components/agora/ComposeBox.tsx`** — Client Component:

Per UI-SPEC:
- Base: `Card` component
- `<textarea>` styled like Input: `bg-navy-light border border-gold/[0.15] rounded-md p-4 font-garamond text-base text-text-light placeholder:text-text-mid`, min-height 80px, auto-grows with content, placeholder: "Share in the Agora..."
- Photo attach button: outline icon button with `Camera` or `Image` icon from lucide-react, 44px touch target
- Photo attachment: on file select, upload to Firebase Storage at `posts/{uid}/{timestamp}_{filename}` using `uploadBytesResumable`, show upload progress, store download URL
- **AI category classification** (per CONTEXT locked decision):
  - Import `getAI, getGenerativeModel, GoogleAIBackend, Schema` from `firebase/ai`
  - Import `firebaseApp` from `@/lib/firebase/client`
  - Initialize: `const ai = getAI(firebaseApp, { backend: new GoogleAIBackend() })` at module scope
  - Create model: `getGenerativeModel(ai, { model: 'gemini-2.0-flash', generationConfig: { responseMimeType: 'text/x.enum', responseSchema: Schema.enumString({ enum: ORTHODOX_CATEGORIES }) } })`
  - After 800ms debounce of text input, if text >= 20 chars, call `model.generateContent(prompt)` where prompt is: `Classify this Eastern Orthodox Christian social media post into exactly one of the provided categories. Post: "${text}"`
  - Show suggested category as chip: "Suggested: {Category}" with "Change" link
  - If text < 20 chars OR AI returns null/error: show "What best describes this post?" with manual category picker
  - "Change" link opens a grid of 10 category chips (each a selectable pill button)
- Submit button: `Button` gold variant, text "Share to Agora", disabled when textarea empty
- On submit: call `createPost(uid, { text, imageUrl, category, commentsRestricted: 'all', linkPreview })` Server Action
- **Link preview**: detect first URL in text with regex `/https?:\/\/[^\s]+/`; if found, call `fetchLinkPreview(url)` Server Action on debounce; store result for submission
- After successful submit: clear textarea, reset category, show new post optimistically at top of feed (call a callback prop `onPostCreated`)

**6. Create `src/components/agora/FeedClient.tsx`** — Client Component:

Per UI-SPEC and research patterns:
- Props: `uid: string`
- State: `posts: Post[]`, `lastDoc: DocumentSnapshot | null`, `hasMore: boolean`, `loading: boolean`, `activeCategory: string | null`, `blockedUsers: string[]`, `mutedUsers: string[]`
- On mount: load blocked/muted user lists from `userBlocks/{uid}/blocked` and `userMutes/{uid}/muted` via Firestore client SDK
- Feed query: `getDocs` (NOT onSnapshot) from `users/{uid}/userFeed` ordered by `createdAt desc`, limit 10, with optional `where('category', '==', activeCategory)` if category filter active, with `startAfter(lastDoc)` for pagination
- Infinite scroll: use `IntersectionObserver` on a sentinel div at bottom of feed; when visible and `hasMore`, load next page
- Filter posts: exclude any post where `authorUid` is in `blockedUsers` or `mutedUsers` arrays (client-side filter)
- Render:
  - `ComposeBox` at top (with `onPostCreated` callback to prepend new post)
  - `CategoryFilterTabs` below compose box
  - `BlockingSkeletons` while loading first page
  - Empty state if no posts: heading "The Agora is quiet", body "Follow other members to see their posts here. Start by exploring profiles."
  - List of `PostCard` components for each post
  - Loading spinner at bottom when loading more pages: gold `animate-spin` SVG
- When category changes: reset posts array, reset lastDoc, reload from start
- **Search input** (AGRA-10): Add state `searchTerm: string`. Render a search bar above `CategoryFilterTabs`: `<input type="search" placeholder="Search posts..." className="w-full bg-navy-light border border-gold/[0.15] rounded-md px-4 py-2 font-garamond text-base text-text-light placeholder:text-text-mid focus:border-gold/40 focus:outline-none mb-3" />`. On input (300ms debounce), if `searchTerm.length >= 3`: query `posts` collection (NOT userFeed) with `where("searchKeywords", "array-contains", searchTerm.toLowerCase())`, ordered by `createdAt desc`, limit 20. Display matching PostCards in place of the normal feed. Show "Searching..." skeleton during query. If no results: "No posts found for \"{searchTerm}\"" in `font-garamond text-text-mid text-center py-8`. When search input is cleared, return to normal feed view. Search and category filter are mutually exclusive — entering a search term clears the active category, selecting a category clears the search term.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/agora/page.tsx contains `getTokens`
    - src/app/(main)/agora/page.tsx contains `FeedClient`
    - src/app/(main)/dashboard/page.tsx contains `redirect('/agora')`
    - src/components/agora/FeedClient.tsx contains `'use client'`
    - src/components/agora/FeedClient.tsx contains `IntersectionObserver`
    - src/components/agora/FeedClient.tsx contains `userFeed`
    - src/components/agora/FeedClient.tsx contains `startAfter`
    - src/components/agora/FeedClient.tsx contains `getDocs`
    - src/components/agora/FeedClient.tsx contains `searchKeywords` or `array-contains` (search query)
    - src/components/agora/FeedClient.tsx contains `Search posts` or `search` placeholder
    - src/components/agora/ComposeBox.tsx contains `firebase/ai` or `getAI`
    - src/components/agora/ComposeBox.tsx contains `Share to Agora`
    - src/components/agora/ComposeBox.tsx contains `gemini-2.0-flash` or `gemini`
    - src/components/agora/ComposeBox.tsx contains `text/x.enum`
    - src/components/agora/CategoryFilterTabs.tsx contains `role="tablist"`
    - src/components/agora/CategoryFilterTabs.tsx contains `ORTHODOX_CATEGORIES`
    - src/components/agora/BlockingSkeletons.tsx contains `animate-pulse`
    - src/components/agora/BlockingSkeletons.tsx contains `aria-busy`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Agora feed page at /agora renders ComposeBox with AI classification, CategoryFilterTabs, infinite scroll FeedClient, skeleton loaders, and dashboard redirects to /agora</done>
</task>

<task type="auto">
  <name>Task 2: PostCard and LinkPreviewCard components</name>
  <files>
    src/components/agora/PostCard.tsx,
    src/components/agora/LinkPreviewCard.tsx
  </files>
  <read_first>
    src/components/ui/Card.tsx,
    src/components/ui/Button.tsx,
    src/lib/types/social.ts,
    src/lib/constants/jurisdictions.ts,
    src/app/actions/likes.ts,
    src/app/actions/posts.ts,
    .planning/phases/02-social-core/02-UI-SPEC.md
  </read_first>
  <action>
**1. Create `src/components/agora/PostCard.tsx`** — Client Component:

Props: `post: Post`, `currentUserUid: string`, `onPostDeleted?: (postId: string) => void`

Per UI-SPEC PostCard spec:
- Base: `Card` component with no extra padding override (use Card's default `p-8`)
- **Header row** (flex, items-center, gap-2):
  - Avatar: 36px circle, `rounded-full border border-gold/[0.15]`. If `post.authorAvatarUrl`, render `<img>`. Else render initial letter circle with `bg-gold-dim text-navy font-cinzel text-sm`
  - Display name: `font-cinzel text-base text-text-light` — `Link` to `/profile/${post.authorHandle}`
  - Verified checkmark: if `post.authorRoleLevel >= 2`, render gold SVG check icon (16px) inline after display name. Use `text-gold` fill.
  - @handle: `font-cinzel text-xs text-text-mid` — `@${post.authorHandle}`
  - Jurisdiction badge: `font-cinzel text-xs text-gold-dim` displayed inline: `\u2022 ${getJurisdictionLabel(post.authorJurisdictionId)}`
  - Relative timestamp: `font-cinzel text-xs text-text-mid` — use a `formatRelativeTime(post.createdAt)` helper (e.g., "2h ago", "3d ago", "Mar 15")
  - "edited" marker: if `post.isEdited`, show `font-garamond text-xs italic text-text-mid` text "edited" after timestamp
- **Three-dot overflow** (only if `post.authorUid === currentUserUid`):
  - `...` button (lucide-react `MoreHorizontal` icon), `text-text-mid hover:text-text-light`
  - `aria-label="Post options"`
  - Dropdown with "Edit" and "Delete" options
  - Delete: opens confirmation dialog (modal) with copy from UI-SPEC: "Delete this post? This cannot be undone." Confirm button: crimson Button "Delete". Cancel: outline Button "Keep". On confirm: call `deletePost(currentUserUid, post.postId)`, then call `onPostDeleted?.(post.postId)`
- **Post text**: `font-garamond text-base text-text-light leading-relaxed mt-3` — `post.text`
- **Optional image**: if `post.imageUrl`, render `<img>` with `rounded-md max-h-96 w-full object-cover mt-3`
- **Link preview**: if `post.linkPreview`, render `LinkPreviewCard` component below text
- **Bottom row** (flex, items-center, justify-between, mt-3):
  - **Category chip**: `bg-navy-light border border-gold/[0.15] font-cinzel text-xs uppercase tracking-widest text-gold-dim px-2 py-0.5 rounded-full` — `post.category`
  - **Like button**: Heart icon from lucide-react. State: `isLiked` (check existence of `posts/{postId}/likes/{currentUserUid}` doc on mount via Firestore client SDK). Idle: `text-text-mid` unfilled heart. Liked: `text-gold` filled heart. Count: `post.likeCount` (use local state for optimistic). `aria-label` toggled: "Like post" / "Unlike post"
  - On click: optimistic toggle (flip heart immediately, +/-1 count), then call `toggleLike(currentUserUid, post.postId)`. On failure: revert, show toast "Couldn't update like. Please try again."
  - **Comment count**: `MessageCircle` icon from lucide-react + count. Wrapped in `Link` to `/agora/${post.postId}`. `text-text-mid hover:text-gold`. `aria-label="View ${post.commentCount} comments"`

**2. Create `src/components/agora/LinkPreviewCard.tsx`** — Client Component:

Props: `preview: LinkPreview`

Per UI-SPEC:
- Container: `bg-navy-light border border-gold/[0.10] rounded-md overflow-hidden mt-3`
- Layout: if `preview.imageUrl`, show thumbnail (120px height, full width, object-cover) at top
- Title: `font-cinzel text-xs text-text-light px-3 pt-2` — `preview.title` (truncate single line)
- Description: `font-garamond text-base text-text-mid px-3 line-clamp-2` — `preview.description`
- Domain: `font-cinzel text-xs text-text-mid px-3 pb-2 pt-1` — extract domain from `preview.url` using `new URL(preview.url).hostname`
- Entire card is clickable: `<a href={preview.url} target="_blank" rel="noopener noreferrer">`
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/agora/PostCard.tsx contains `'use client'`
    - src/components/agora/PostCard.tsx contains `getJurisdictionLabel`
    - src/components/agora/PostCard.tsx contains `toggleLike`
    - src/components/agora/PostCard.tsx contains `authorRoleLevel >= 2` or verified check logic
    - src/components/agora/PostCard.tsx contains `aria-label`
    - src/components/agora/PostCard.tsx contains `Delete this post`
    - src/components/agora/PostCard.tsx contains `MoreHorizontal` or `more-horizontal` (three-dot icon)
    - src/components/agora/PostCard.tsx contains `Link` to `/agora/` (comment count link)
    - src/components/agora/PostCard.tsx contains `font-cinzel` and `font-garamond` (Byzantine typography)
    - src/components/agora/LinkPreviewCard.tsx contains `line-clamp-2`
    - src/components/agora/LinkPreviewCard.tsx contains `target="_blank"`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>PostCard renders complete post layout per UI-SPEC with jurisdiction badge, verified checkmark, optimistic like, delete confirmation, three-dot menu, and category chip; LinkPreviewCard renders OG metadata</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- /agora page renders with ComposeBox, CategoryFilterTabs, and FeedClient
- Dashboard redirects to /agora
- PostCard shows all required elements per UI-SPEC
- AI category classification connected via firebase/ai
</verification>

<success_criteria>
- Complete Agora feed experience: compose, filter, scroll, interact
- AI category suggestion works via Firebase AI Logic client-side SDK
- Infinite scroll loads 10 posts per page with cursor pagination
- Post cards display all required metadata: avatar, name, handle, jurisdiction, timestamp, category, like/comment counts
- Like/unlike is optimistic with rollback on failure
- Dashboard redirects authenticated users to /agora
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-core/02-04-SUMMARY.md`
</output>
