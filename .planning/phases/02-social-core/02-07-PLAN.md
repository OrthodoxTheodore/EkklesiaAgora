---
phase: 02-social-core
plan: "07"
type: execute
wave: 5
depends_on: ["02-06"]
files_modified: []
autonomous: false
requirements: [PROF-04, AGRA-05, AGRA-07, AGRA-08, CAT-02]

must_haves:
  truths:
    - "End-to-end social flow works: create profile, follow user, create post, see it in feed, like, comment"
    - "Notification bell updates in real-time when someone interacts with your content"
    - "Category filtering narrows the feed correctly"
    - "Profile pages display jurisdiction badge prominently"
    - "Byzantine aesthetic is maintained throughout all new pages"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 2 social experience — profile creation, Agora feed, post creation, likes, comments, notifications, and block/mute.

Purpose: Verify the integrated social experience works end-to-end before marking Phase 2 complete.
Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-social-core/02-CONTEXT.md
@.planning/phases/02-social-core/02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 2 Social Core — user profiles, Agora feed, post CRUD, likes, comments, notifications, block/mute, AI category classification, and category filtering</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:3000 in your browser

    **Profile Flow:**
    3. Log in with your account
    4. You should be redirected to /agora (dashboard now redirects here)
    5. Navigate to /profile/edit
    6. Set your display name, @handle, bio, and select a jurisdiction from the dropdown
    7. Verify the jurisdiction dropdown has two sections: "Canonical Eastern Orthodox Churches" and "Other Christians"
    8. Upload an avatar image — verify it shows after upload
    9. Save your profile
    10. Navigate to your own profile page (link in navbar avatar dropdown)
    11. Verify: banner area, circular avatar, display name in gold, @handle, jurisdiction badge pill, bio text, stats row (0 Posts, 0 Followers, 0 Following)

    **Post Creation Flow:**
    12. Go to /agora
    13. Type a post about a church topic (at least 20 characters)
    14. Wait ~1 second — an AI-suggested category chip should appear below the textarea (e.g., "Suggested: Spiritual Life")
    15. Click "Change" to see the manual category picker
    16. Select a different category if desired
    17. Click "Share to Agora" — post should appear in your feed immediately
    18. Verify the post card shows: your avatar, display name, @handle, jurisdiction badge, relative timestamp, post text, category chip, like count (0), comment count (0)

    **Interaction Flow:**
    19. Click the heart icon on your post — it should fill gold and count should increment to 1
    20. Click heart again — it should return to empty and count should decrement to 0
    21. Click the comment count link — should navigate to /agora/{postId}
    22. On the post detail page, type a comment and submit
    23. Verify the comment appears immediately below the post
    24. Test edit: click edit on your comment, change text, save — verify "edited" marker appears
    25. Test delete: click delete on your comment, verify confirmation dialog appears, confirm deletion

    **Category Filtering:**
    26. Go back to /agora
    27. Create 2-3 posts in different categories
    28. Click a category tab at the top (e.g., "Holy Scripture") — verify only posts with that category show
    29. Click "All" — verify all posts show again

    **Notification Flow:**
    30. If you have a second test account: log in on another browser, follow the first user, like their post
    31. Switch back to the first account — verify the bell icon shows an unread count badge
    32. Click the bell — verify the notification dropdown shows the like/follow notifications
    33. Click "Mark all read" — verify unread count clears

    **Block/Mute (if second account available):**
    34. Navigate to the second user's profile
    35. Click the three-dot menu — verify Block, Mute, Report User options
    36. Test Block: verify confirmation dialog text matches, confirm block, verify redirect to /agora

    **Visual Verification:**
    37. Check all pages maintain Byzantine aesthetic: navy backgrounds, gold accents, Cinzel headings, EB Garamond body text
    38. Check mobile responsiveness: resize browser to mobile width, verify navbar hamburger, profile header, and feed cards render correctly
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of all Phase 2 success criteria from ROADMAP.md:
1. A user can set their display name, avatar, bio, and jurisdiction; other users can view their public profile
2. A user can create text posts with optional photo attachments, like and comment on posts, and delete their own posts
3. A user can see an activity feed of posts from people and channels they follow, with link preview cards on shared URLs
4. A user receives in-app notifications (bell) for likes, comments, follows, and mentions
5. A user can block or mute others, and can search posts by keyword
</verification>

<success_criteria>
User approves the complete social experience or provides specific feedback for gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-core/02-07-SUMMARY.md`
</output>
