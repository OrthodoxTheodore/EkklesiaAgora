---
phase: 02-social-core
plan: "06"
type: execute
wave: 5
depends_on: ["02-04", "02-05"]
files_modified:
  - src/components/nav/NotificationBell.tsx
  - src/components/nav/Navbar.tsx
  - src/components/nav/MobileMenu.tsx
  - src/components/profile/ProfileHeader.tsx
autonomous: true
requirements: [AGRA-06, AGRA-07, AGRA-08]

must_haves:
  truths:
    - "A user can see a notification bell in the navbar with unread count badge"
    - "Clicking the bell shows a dropdown of recent notifications (likes, comments, follows, mentions)"
    - "Notifications update in real-time via onSnapshot"
    - "Block and mute actions on profiles are functional end-to-end"
    - "Navbar shows user avatar and links to profile"
  artifacts:
    - path: "src/components/nav/NotificationBell.tsx"
      provides: "Bell icon with unread badge and notification dropdown"
      contains: "onSnapshot"
    - path: "src/components/nav/Navbar.tsx"
      provides: "Updated navbar with NotificationBell and profile avatar link"
      contains: "NotificationBell"
  key_links:
    - from: "src/components/nav/NotificationBell.tsx"
      to: "users/{uid}/notifications"
      via: "onSnapshot real-time listener"
      pattern: "onSnapshot"
    - from: "src/components/profile/ProfileHeader.tsx"
      to: "src/app/actions/moderation.ts"
      via: "blockUser, muteUser calls"
      pattern: "blockUser\\("
---

<objective>
Wire up the notification bell in the navbar, connect block/mute actions on profile pages, and update the navbar to show user avatar and profile link.

Purpose: Notifications complete the social feedback loop. Block/mute give users control over their experience.
Output: Real-time notification bell, functional block/mute on profiles, updated navbar with avatar.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-social-core/02-CONTEXT.md
@.planning/phases/02-social-core/02-UI-SPEC.md
@.planning/phases/02-social-core/02-01-SUMMARY.md
@.planning/phases/02-social-core/02-03-SUMMARY.md
@.planning/phases/02-social-core/02-04-SUMMARY.md

<interfaces>
From src/app/actions/notifications.ts:
```typescript
export async function markNotificationsRead(uid: string): Promise<{ success, count }>;
export async function markSingleNotificationRead(uid: string, notificationId: string): Promise<void>;
```

From src/app/actions/moderation.ts:
```typescript
export async function blockUser(callerUid: string, targetUid: string): Promise<{ success }>;
export async function muteUser(callerUid: string, targetUid: string): Promise<{ success }>;
export async function unblockUser(callerUid: string, targetUid: string): Promise<void>;
export async function unmuteUser(callerUid: string, targetUid: string): Promise<void>;
export async function reportContent(callerUid: string, data: { contentType, contentId, reason }): Promise<{ success }>;
```

From src/lib/types/social.ts:
```typescript
export interface Notification { notificationId, type: 'like' | 'comment' | 'follow' | 'mention', fromUid, fromHandle, fromDisplayName, fromAvatarUrl, postId, postText, read, createdAt }
```

From src/components/nav/Navbar.tsx (current):
```typescript
export function Navbar() {
  const { user, roleLevel } = useAuth();
  // ... avatar dropdown with Profile, Dashboard, Admin, Sign Out links
}
```

From src/components/auth/AuthProvider.tsx:
```typescript
export function useAuth(): { user: User | null; loading: boolean; roleLevel: RoleLevel };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationBell component with real-time updates</name>
  <files>
    src/components/nav/NotificationBell.tsx
  </files>
  <read_first>
    src/components/nav/Navbar.tsx,
    src/lib/firebase/client.ts,
    src/lib/types/social.ts,
    src/components/auth/AuthProvider.tsx,
    .planning/phases/02-social-core/02-UI-SPEC.md
  </read_first>
  <action>
**Create `src/components/nav/NotificationBell.tsx`** — Client Component (`'use client'`):

Props: `uid: string`

Per UI-SPEC NotificationBell:
- **Bell icon**: `Bell` from lucide-react, 24px, `text-text-light hover:text-gold`
- **Unread badge**: if unreadCount > 0, show 16px circle `bg-gold text-navy font-cinzel text-xs` absolute positioned top-right of bell
- **Touch target**: 44px x 44px button wrapping the bell icon
- **`aria-label`**: "Notifications, {N} unread" or "Notifications, no unread"

**Real-time listener** (per research Pattern 7):
- `useEffect` with `onSnapshot` on `collection(db, 'users', uid, 'notifications')` where `read == false`, ordered by `createdAt desc`, limit 50
- Import: `collection, query, where, onSnapshot, orderBy, limit` from `firebase/firestore`
- Import: `getFirestore` from `firebase/firestore`, `firebaseApp` from `@/lib/firebase/client`
- Set `unreadCount` from `snap.size`
- Set `notifications` array from `snap.docs.map(d => ({ notificationId: d.id, ...d.data() }) as Notification)`
- Cleanup: return `unsub()` from useEffect

**Dropdown** (toggle on bell click):
- Container: `absolute right-0 top-full mt-2 w-80 max-h-96 overflow-y-auto bg-navy-mid border border-gold/20 rounded shadow-xl z-60`
- Header: "Notifications" in `font-cinzel text-xs uppercase tracking-widest text-gold px-4 py-3 border-b border-gold/[0.10]`
- "Mark all read" button in header: `font-cinzel text-xs text-text-mid hover:text-gold cursor-pointer` — on click call `markNotificationsRead(uid)` Server Action
- Each notification item:
  - Avatar (28px circle) + message text + relative timestamp
  - Message format by type:
    - `like`: "{fromDisplayName} liked your post" (truncated postText)
    - `comment`: "{fromDisplayName} commented on your post"
    - `follow`: "{fromDisplayName} started following you"
    - `mention`: "{fromDisplayName} mentioned you in a post"
  - Text: `font-garamond text-sm text-text-light` for name, `text-text-mid` for action text
  - Timestamp: `font-cinzel text-xs text-text-mid`
  - Unread items: `bg-navy-light/50` background
  - Click: navigate to relevant page (post page for like/comment/mention, profile for follow) and mark as read
- Empty state: heading "No notifications yet", body "You'll see likes, comments, follows, and mentions here." — `font-garamond text-sm text-text-mid text-center py-8`
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nav/NotificationBell.tsx contains `'use client'`
    - src/components/nav/NotificationBell.tsx contains `onSnapshot`
    - src/components/nav/NotificationBell.tsx contains `read.*==.*false` or `where('read', '==', false)`
    - src/components/nav/NotificationBell.tsx contains `markNotificationsRead`
    - src/components/nav/NotificationBell.tsx contains `Bell` (lucide icon import)
    - src/components/nav/NotificationBell.tsx contains `aria-label`
    - src/components/nav/NotificationBell.tsx contains `bg-gold text-navy` (unread badge)
    - src/components/nav/NotificationBell.tsx contains `No notifications yet`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>NotificationBell renders real-time unread count badge, dropdown with notification items grouped by type, mark-all-read action, and navigation to relevant pages</done>
</task>

<task type="auto">
  <name>Task 2: Navbar integration, profile block/mute wiring, and profile post content</name>
  <files>
    src/components/nav/Navbar.tsx,
    src/components/nav/MobileMenu.tsx,
    src/components/profile/ProfileHeader.tsx
  </files>
  <read_first>
    src/components/nav/Navbar.tsx,
    src/components/nav/MobileMenu.tsx,
    src/components/profile/ProfileHeader.tsx,
    src/components/nav/NotificationBell.tsx,
    src/app/actions/moderation.ts,
    src/app/actions/follows.ts,
    .planning/phases/02-social-core/02-UI-SPEC.md,
    .planning/phases/02-social-core/02-CONTEXT.md
  </read_first>
  <action>
**1. Update `src/components/nav/Navbar.tsx`**:

- Import `NotificationBell` from `./NotificationBell`
- In the desktop auth section (where the avatar circle button is), add `NotificationBell` BEFORE the avatar button:
  ```
  {user && <NotificationBell uid={user.uid} />}
  ```
- Update the "Profile" link in the avatar dropdown to point to `/profile/edit` (or later, the user's own profile handle — for now `/profile/edit` is safest)
- Remove "Dashboard" link from dropdown (dashboard now redirects to /agora)
- Add "Agora" link if not already visible in the dropdown
- Keep existing Admin link (for admin+ roles) and Sign Out button

**2. Update `src/components/nav/MobileMenu.tsx`**:

- Add NotificationBell to mobile menu header area
- Ensure Agora link is prominent in mobile navigation

**3. Update `src/components/profile/ProfileHeader.tsx`**:

Wire the three-dot overflow menu actions (Block, Mute, Report) to actual Server Actions:

- **Follow button**: import `followUser` and `unfollowUser` from `@/app/actions/follows`. On click:
  - If not following: call `followUser(currentUserUid, profile.uid)`, optimistically switch to "Following" state
  - If following: call `unfollowUser(currentUserUid, profile.uid)`, optimistically switch to "Follow" state
  - On failure: revert, show toast "Could not follow. Try again." (per UI-SPEC interaction contract)

- **Three-dot overflow menu** (Block, Mute, Report):
  - **Block**: on click, show confirmation dialog: "Block {displayName}? They won't be able to see your posts or follow you." Confirm: "Block {displayName}" button. Cancel: "Don't Block" button. On confirm: call `blockUser(currentUserUid, profile.uid)`, navigate away to `/agora` (per UI-SPEC interaction contract)
  - **Mute**: on click, call `muteUser(currentUserUid, profile.uid)`, show toast "Muted {displayName}. Their posts will be hidden from your feed."
  - **Report User**: on click, open a small sheet/modal with reason selection (e.g., "Spam", "Harassment", "Inappropriate content", "Other"). On submit: call `reportContent(currentUserUid, { contentType: 'user', contentId: profile.uid, reason })`. On success: close sheet, show toast "Report submitted. Thank you." (per UI-SPEC copy)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nav/Navbar.tsx contains `NotificationBell`
    - src/components/nav/Navbar.tsx contains `user.uid` passed to NotificationBell
    - src/components/nav/Navbar.tsx does NOT contain `Dashboard` link (removed)
    - src/components/profile/ProfileHeader.tsx contains `blockUser`
    - src/components/profile/ProfileHeader.tsx contains `muteUser`
    - src/components/profile/ProfileHeader.tsx contains `reportContent`
    - src/components/profile/ProfileHeader.tsx contains `followUser`
    - src/components/profile/ProfileHeader.tsx contains `unfollowUser`
    - src/components/profile/ProfileHeader.tsx contains `Block ${profile.displayName}` or template string for block confirmation
    - src/components/profile/ProfileHeader.tsx contains `Report submitted`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Navbar shows NotificationBell with real-time unread count; profile block/mute/report are wired to Server Actions with confirmations and toasts; follow/unfollow is optimistic with error rollback</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- NotificationBell appears in navbar for authenticated users
- Notifications dropdown shows grouped items with real-time updates
- Block confirmation dialog matches UI-SPEC copy
- Follow button toggles optimistically
</verification>

<success_criteria>
- Notification bell shows real-time unread count via Firestore onSnapshot
- Clicking bell opens dropdown with notification items (like, comment, follow, mention)
- Mark-all-read clears unread count
- Block/mute on profile pages work end-to-end
- Report user submits to moderation queue
- Follow/unfollow is optimistic with toast on failure
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-core/02-06-SUMMARY.md`
</output>
