---
phase: 02-social-core
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/actions/posts.test.ts
  - tests/actions/comments.test.ts
  - tests/actions/profile.test.ts
  - tests/actions/linkPreview.test.ts
  - tests/lib/posts.test.ts
  - tests/components/PostCard.test.tsx
  - tests/components/FeedClient.test.tsx
  - tests/components/NotificationBell.test.tsx
  - tests/components/CategoryFilterTabs.test.tsx
  - tests/components/JurisdictionDropdown.test.tsx
  - tests/profile/page.test.tsx
  - tests/profile/edit.test.tsx
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, AGRA-01, AGRA-02, AGRA-03, AGRA-04, AGRA-05, AGRA-06, AGRA-07, AGRA-08, AGRA-09, AGRA-10, CAT-01, CAT-02]

must_haves:
  truths:
    - "All 12 test stub files exist and pass with jest --passWithNoTests or as placeholder stubs"
    - "Running npx jest --testPathPattern='(posts|profiles|comments|follows|notifications)' --passWithNoTests exits 0"
    - "Each stub file contains at least one describe block and one placeholder test"
  artifacts:
    - path: "tests/actions/posts.test.ts"
      provides: "Stub tests for AGRA-01, AGRA-09, CAT-01"
    - path: "tests/actions/comments.test.ts"
      provides: "Stub tests for AGRA-04"
    - path: "tests/actions/profile.test.ts"
      provides: "Stub tests for PROF-01, PROF-06"
    - path: "tests/actions/linkPreview.test.ts"
      provides: "Stub tests for AGRA-08"
    - path: "tests/lib/posts.test.ts"
      provides: "Stub tests for AGRA-10"
    - path: "tests/components/PostCard.test.tsx"
      provides: "Stub tests for AGRA-02, AGRA-03"
    - path: "tests/components/FeedClient.test.tsx"
      provides: "Stub tests for AGRA-05, AGRA-06"
    - path: "tests/components/NotificationBell.test.tsx"
      provides: "Stub tests for AGRA-07"
    - path: "tests/components/CategoryFilterTabs.test.tsx"
      provides: "Stub tests for CAT-02"
    - path: "tests/components/JurisdictionDropdown.test.tsx"
      provides: "Stub tests for PROF-02, PROF-03"
    - path: "tests/profile/page.test.tsx"
      provides: "Stub tests for PROF-04"
    - path: "tests/profile/edit.test.tsx"
      provides: "Stub tests for PROF-05"
  key_links: []
---

<objective>
Create all 12 test stub files required by the Phase 2 Validation Strategy (02-VALIDATION.md) so that subsequent plans can chain jest test commands in their `<automated>` verify steps.

Purpose: Nyquist compliance requires that every task has an automated verification command. The test files referenced in VALIDATION.md must exist as stubs before any Wave 1+ plan runs, otherwise jest commands will fail with "file not found."
Output: 12 test stub files, each containing a describe block with placeholder `test.todo()` entries for the requirements they cover.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-social-core/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 12 test stub files with placeholder describe blocks</name>
  <files>
    tests/actions/posts.test.ts,
    tests/actions/comments.test.ts,
    tests/actions/profile.test.ts,
    tests/actions/linkPreview.test.ts,
    tests/lib/posts.test.ts,
    tests/components/PostCard.test.tsx,
    tests/components/FeedClient.test.tsx,
    tests/components/NotificationBell.test.tsx,
    tests/components/CategoryFilterTabs.test.tsx,
    tests/components/JurisdictionDropdown.test.tsx,
    tests/profile/page.test.tsx,
    tests/profile/edit.test.tsx
  </files>
  <read_first>
    jest.config.ts,
    .planning/phases/02-social-core/02-VALIDATION.md
  </read_first>
  <action>
Create the `tests/actions/`, `tests/lib/`, `tests/components/`, and `tests/profile/` directories if they do not exist.

Create each file with the following pattern — a describe block, requirement ID comments, and `test.todo()` placeholders that will be filled in by the plan that implements the corresponding feature. Each file should import nothing and have no real assertions yet.

**1. `tests/actions/posts.test.ts`**:
```typescript
// Stubs for AGRA-01, AGRA-09, CAT-01
describe('Post Server Actions', () => {
  test.todo('createPost creates a post document in Firestore');
  test.todo('createPost fans out to follower userFeed subcollections');
  test.todo('createPost validates category is one of ORTHODOX_CATEGORIES');
  test.todo('deletePost removes post and all subcollection data');
  test.todo('editPost updates text and marks isEdited true');
  test.todo('createPost builds searchKeywords from lowercased text tokens');
});
```

**2. `tests/actions/comments.test.ts`**:
```typescript
// Stubs for AGRA-04
describe('Comment Server Actions', () => {
  test.todo('createComment adds a comment to posts/{postId}/comments');
  test.todo('createComment increments commentCount on the post');
  test.todo('createComment respects follower-only restriction');
  test.todo('editComment updates text and sets isEdited true');
  test.todo('deleteComment removes comment and decrements count');
});
```

**3. `tests/actions/profile.test.ts`**:
```typescript
// Stubs for PROF-01, PROF-06
describe('Profile Server Actions', () => {
  test.todo('updateProfile validates and writes to Firestore');
  test.todo('updateProfile rejects duplicate handles');
  test.todo('updateAvatar updates avatarUrl on the profile');
  test.todo('updateBanner updates bannerUrl on the profile');
  test.todo('registerUser creates a userProfiles document with default handle');
});
```

**4. `tests/actions/linkPreview.test.ts`**:
```typescript
// Stubs for AGRA-08
describe('Link Preview Server Action', () => {
  test.todo('fetchLinkPreview returns OG metadata for a valid URL');
  test.todo('fetchLinkPreview returns null for an invalid URL');
  test.todo('fetchLinkPreview times out after 5 seconds');
});
```

**5. `tests/lib/posts.test.ts`**:
```typescript
// Stubs for AGRA-10
describe('Post query helpers', () => {
  test.todo('buildSearchKeywords splits text into lowercased tokens >= 3 chars');
  test.todo('buildSearchKeywords deduplicates tokens');
  test.todo('getPost returns null for non-existent post');
});
```

**6. `tests/components/PostCard.test.tsx`**:
```typescript
// Stubs for AGRA-02, AGRA-03
describe('PostCard component', () => {
  test.todo('renders avatar, display name, handle, and jurisdiction badge');
  test.todo('renders verified checkmark when authorRoleLevel >= 2');
  test.todo('renders category chip with post category');
  test.todo('like button toggles optimistically on click');
  test.todo('comment count links to /agora/{postId}');
  test.todo('shows edit/delete menu only for own posts');
});
```

**7. `tests/components/FeedClient.test.tsx`**:
```typescript
// Stubs for AGRA-05, AGRA-06
describe('FeedClient component', () => {
  test.todo('renders loading skeletons on initial mount');
  test.todo('renders empty state when no posts exist');
  test.todo('filters out posts from blocked users');
  test.todo('filters out posts from muted users');
  test.todo('loads more posts on scroll to bottom');
});
```

**8. `tests/components/NotificationBell.test.tsx`**:
```typescript
// Stubs for AGRA-07
describe('NotificationBell component', () => {
  test.todo('renders bell icon with aria-label');
  test.todo('shows unread count badge when notifications exist');
  test.todo('opens dropdown on click');
  test.todo('mark all read clears unread count');
});
```

**9. `tests/components/CategoryFilterTabs.test.tsx`**:
```typescript
// Stubs for CAT-02
describe('CategoryFilterTabs component', () => {
  test.todo('renders All tab plus all 10 ORTHODOX_CATEGORIES');
  test.todo('highlights active category tab');
  test.todo('calls onCategoryChange when tab clicked');
  test.todo('uses role=tablist and aria-selected attributes');
});
```

**10. `tests/components/JurisdictionDropdown.test.tsx`**:
```typescript
// Stubs for PROF-02, PROF-03
describe('JurisdictionDropdown component', () => {
  test.todo('renders all 18 canonical Orthodox jurisdictions');
  test.todo('renders all 4 Other Christian options');
  test.todo('displays two separated sections');
  test.todo('includes both OCU and UOC in the list');
});
```

**11. `tests/profile/page.test.tsx`**:
```typescript
// Stubs for PROF-04
describe('Profile page', () => {
  test.todo('renders profile header with banner, avatar, display name');
  test.todo('shows jurisdiction badge prominently');
  test.todo('shows follow button for non-own profiles');
  test.todo('shows edit profile button for own profile');
  test.todo('returns 404 for non-existent handle');
});
```

**12. `tests/profile/edit.test.tsx`**:
```typescript
// Stubs for PROF-05
describe('Profile edit page', () => {
  test.todo('renders form with display name, handle, bio, jurisdiction, patron saint');
  test.todo('validates handle format (lowercase, numbers, underscores)');
  test.todo('shows avatar upload component');
  test.todo('submits to updateProfile Server Action');
});
```

After creating all files, run `npx jest --testPathPattern="(posts|profiles|comments|follows|notifications|PostCard|FeedClient|NotificationBell|CategoryFilter|Jurisdiction|profile)" --passWithNoTests` to confirm all stubs are found and pass.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="(posts|comments|profile|linkPreview|PostCard|FeedClient|NotificationBell|CategoryFilter|Jurisdiction)" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All 12 test stub files exist in their respective directories, each with a describe block and test.todo() placeholders. Jest discovers all files and exits 0.</done>
</task>

</tasks>

<verification>
- All 12 files listed in 02-VALIDATION.md Wave 0 Requirements exist
- `npx jest --passWithNoTests` exits with code 0
- Each file has at least one `describe` block and one `test.todo`
</verification>

<success_criteria>
- Nyquist compliance: every subsequent plan can now chain a jest command in its verify step
- VALIDATION.md Wave 0 checklist items are all satisfiable
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-core/02-00-SUMMARY.md`
</output>
