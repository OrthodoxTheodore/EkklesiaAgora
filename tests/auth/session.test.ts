import '@testing-library/jest-dom';

describe('Session persistence (AUTH-02)', () => {
  test.todo('POST /api/login sets HttpOnly session cookie');
  test.todo('authenticated user stays logged in after page refresh');
});
