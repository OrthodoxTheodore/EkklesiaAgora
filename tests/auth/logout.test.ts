import '@testing-library/jest-dom';

describe('Logout (AUTH-04)', () => {
  test.todo('logout clears client auth state');
  test.todo('POST /api/logout clears session cookie');
  test.todo('user is redirected to home after logout');
});
